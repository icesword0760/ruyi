# Playwright Specification

> Generated by spec-gen v1.0.0 on 2026-03-15 00:25
> Source files: ai_service/node_modules/playwright-core/types/protocol.d.ts

## Purpose

Core orchestration service that manages browser engines, tracks all open pages and browsers, and
provides access to different browser channel implementations (Chromium, Firefox, WebKit, Electron,
Android).

## Entities

### InspectorIssue

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents an accessibility, quality, or
other issue found during page inspection/auditing.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| code | InspectorIssueCode | The type/category of the issue |
| details | InspectorIssueDetails | Additional details specific to the issue type |
| issueId | IssueId | Unique identifier for this issue (optional) |

### CreditCard

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Credit card information used for
autofilling payment forms.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| number | string | 16-digit credit card number |
| name | string | Name of the credit card owner |
| expiryMonth | string | 2-digit expiry month |
| expiryYear | string | 4-digit expiry year |
| cvc | string | 3-digit card verification code |

### AddressField

[PARTIAL SPEC — file too large to fully analyze (44 parts)] A single field within an address (e.g.,
name, value).

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| name | string | Address field name (e.g., GIVEN_NAME, FAMILY_NAME) |
| value | string | Address field value |

### Address

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Complete address with multiple fields
for autofilling forms.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| fields | AddressField[] | Fields and values defining an address |

### AddressUI

[PARTIAL SPEC — file too large to fully analyze (44 parts)] UI representation of an address for
display in user interfaces, organized as a two-dimensional array where each inner array represents
an address line.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| addressFields | AddressFields[] | Two-dimensional array containing the representation of values from an address profile |

### FilledField

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Information about a form field that was
autofilled, including its type, value, and filling strategy.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| htmlType | string | The type of the field (e.g., text, password) |
| id | string | The HTML id |
| name | string | The HTML name |
| value | string | The field value |
| autofillType | string | The actual field type (e.g., FAMILY_NAME) |
| fillingStrategy | FillingStrategy | The filling strategy used |
| frameId | Page.FrameId | The frame the field belongs to |
| fieldId | DOM.BackendNodeId | The form field's DOM node |

### BackgroundServiceEvent

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Event from a background web platform
service (e.g., background fetch, push messaging, notifications).

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| timestamp | Network.TimeSinceEpoch | Timestamp of the event (in seconds) |
| origin | string | The origin this event belongs to |
| serviceWorkerRegistrationId | ServiceWorker.RegistrationID | The Service Worker ID that initiated the event |
| service | ServiceName | The Background Service this event belongs to |
| eventName | string | A description of the event |
| instanceId | string | An identifier that groups related events together |
| eventMetadata | EventMetadata[] | A list of event-specific information |
| storageKey | string | Storage key this event belongs to |

### EventMetadata

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Key-value pair for additional background
service event information.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| key | string | Metadata key |
| value | string | Metadata value |

### WindowBounds

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents the position, size and state
of a browser window.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| left | number | The offset from the left edge of the screen to the window in pixels |
| top | number | The offset from the top edge of the screen to the window in pixels |
| width | number | The window width in pixels |
| height | number | The window height in pixels |
| windowState | WindowState | The window state - normal, minimized, maximized, or fullscreen |

### Permission

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents a browser permission
descriptor that defines what capability is being granted or denied.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| name | string | Name of the permission (e.g., camera, geolocation, notifications) |
| sysex | boolean | For midi permission, specifies sysex control |
| userVisibleOnly | boolean | For push permission, specifies if notifications are visible |
| allowWithoutSanitization | boolean | For clipboard permission, allows without sanitization |
| allowWithoutGesture | boolean | For fullscreen permission, allows without user gesture |
| panTiltZoom | boolean | For camera permission, specifies pan tilt zoom support |

### Histogram

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents Chrome browser performance
histogram data for telemetry analysis.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| name | string | The histogram name identifier |
| sum | number | Sum of all sample values |
| count | number | Total number of samples collected |
| buckets | Bucket[] | Array of histogram buckets with value ranges and counts |

**Relationships:**

- contains Bucket (Histogram contains multiple buckets)

### Bucket

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents a single bucket in a Chrome
histogram, defining a range of values and sample count.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| low | number | Minimum value inclusive for this bucket |
| high | number | Maximum value exclusive for this bucket |
| count | number | Number of samples falling within this bucket range |

**Relationships:**

- belongs to Histogram (Bucket belongs to a parent histogram)

### Download

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents a file download event in
progress or completed, with metadata and progress tracking.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| frameId | Page.FrameId | Id of the frame that caused the download to begin |
| guid | string | Global unique identifier of the download |
| url | string | URL of the resource being downloaded |
| suggestedFilename | string | Suggested file name for the download |
| totalBytes | number | Total expected bytes to download |
| receivedBytes | number | Total bytes received so far |
| state | string | Download status: inProgress, completed, or canceled |
| filePath | string | Path of downloaded file when completed |

### Cache

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents a browser cache storage
object used in the Cache Storage API.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| cacheId | CacheId | Opaque unique identifier of the cache |
| securityOrigin | string | Security origin of the cache |
| storageKey | string | Storage key of the cache |
| storageBucket | StorageBucket | Storage bucket of the cache |
| cacheName | string | The name of the cache |

### DataEntry

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents a single data entry within a
cache storage.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| requestURL | string | Request URL |
| requestMethod | string | Request method |
| requestHeaders | Header[] | Request headers |
| responseTime | number | Number of seconds since epoch |
| responseStatus | number | HTTP response status code |
| responseStatusText | string | HTTP response status text |
| responseType | CachedResponseType | HTTP response type |
| responseHeaders | Header[] | Response headers |

**Relationships:**

- belongs to Cache (Data entry belongs to a cache)

### CachedResponse

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents a cached HTTP response
retrieved from cache storage.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| body | binary | Entry content, base64-encoded |

### CastSink

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents a casting device or software
surface that content can be mirrored to.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| name | string | Sink name |
| id | string | Sink identifier |
| session | string | Text describing the current session |

### DOMNode

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents a DOM node in the browser's
document object model, used for browser automation and testing.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| nodeId | NodeId | Unique DOM node identifier |
| parentId | NodeId | The id of the parent node |
| backendNodeId | BackendNodeId | Backend node identifier |
| nodeType | number | Node's nodeType |
| nodeName | string | Node's nodeName |
| localName | string | Node's localName |
| nodeValue | string | Node's nodeValue |
| childNodeCount | number | Child count for container nodes |
| children | Node[] | Child nodes when requested |
| attributes | string[] | Attributes of Element node as flat array |
| documentURL | string | Document URL for Document or FrameOwner nodes |
| frameId | Page.FrameId | Frame ID for frame owner elements |
| contentDocument | Node | Content document for frame owner elements |
| shadowRoots | Node[] | Shadow root list for element host |
| pseudoType | PseudoType | Pseudo element type |
| shadowRootType | ShadowRootType | Shadow root type |
| isSVG | boolean | Whether the node is SVG |

**Relationships:**

- parent-of DOMNode (Node can have child nodes)
- contains DOMNode (Node can contain shadow roots)
- belongs to Page (Frame owner nodes reference a Page/Frame)

### BoxModel

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents the CSS box model of a DOM
element including content, padding, border, and margin.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| content | Quad | Content box coordinates |
| padding | Quad | Padding box coordinates |
| border | Quad | Border box coordinates |
| margin | Quad | Margin box coordinates |
| width | number | Node width |
| height | number | Node height |
| shapeOutside | ShapeOutsideInfo | Shape outside coordinates |

**Relationships:**

- describes DOMNode (BoxModel describes the layout of a DOM node)

### RGBA

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents an RGBA color value.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| r | number | Red component in range 0-255 |
| g | number | Green component in range 0-255 |
| b | number | Blue component in range 0-255 |
| a | number | Alpha component in range 0-1 |

### Rect

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents a rectangle in 2D space.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| x | number | X coordinate |
| y | number | Y coordinate |
| width | number | Rectangle width |
| height | number | Rectangle height |

### BackendNode

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents a backend DOM node with a
friendly name, used for referencing nodes that may not have been pushed to the client.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| nodeType | number | Node's nodeType |
| nodeName | string | Node's nodeName |
| backendNodeId | BackendNodeId | Backend node identifier |

**Relationships:**

- mirrors DOMNode (BackendNode mirrors a DOM node on the backend)

### DetachedElementInfo

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Structure holding a top-level detached
DOM node and its retained descendant node IDs.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| treeNode | Node | The top-level node of a detached tree |
| retainedNodeIds | NodeId[] | Array of retained descendant node IDs |

**Relationships:**

- contains DOMNode (Contains detached node and references to retained nodes)

### CSSComputedStyleProperty

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents a computed CSS style property
for a DOM element.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| name | string | Computed style property name |
| value | string | Computed style property value |

**Relationships:**

- computed-for DOMNode (Style property is computed for a DOM node)

### DOMSearchSession

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents a search session for finding
DOM nodes by text content, selector, or XPath query.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| searchId | string | Unique identifier for the search session |
| resultCount | number | Total number of matching nodes found |
| query | string | The search query (plain text, CSS selector, or XPath) |

**Relationships:**

- contains-results DOMNode (Search session contains matching nodeIds)

### DOMEvent

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents events fired by the DOM
domain when the document structure changes.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| type | string | Event type name |
| nodeId | NodeId | Id of the affected node |
| name | string | Name of the changed attribute or property |
| value | string | New value of the changed attribute |
| characterData | string | New text value for text nodes |
| parentNodeId | NodeId | Parent node id for child events |
| previousNodeId | NodeId | Previous sibling node id for insertions |
| node | DOMNode | The inserted or new node data |
| childNodeCount | number | New child node count for count update events |
| hostId | NodeId | Host element id for shadow DOM events |
| rootId | NodeId | Shadow root id for shadow DOM events |
| root | DOMNode | Shadow root node data |

**Relationships:**

- originates-from DOMNode (Event originates from changes to a DOM node)

### ComputedStyleProperty

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents a computed CSS style property
for a DOM node.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| name | string | Computed style property name (e.g., 'display', 'color') |
| value | string | Computed style property value |

**Relationships:**

- applies-to DOMNode (Computed style applies to a DOM node)

### DOMSnapshot

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents a frozen capture of the
document's DOM tree including layout and computed styles. Used for diffing DOM state in tests.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| domNodes | DOMNode[] | Nodes in the DOM tree, index 0 is root document |
| layoutTreeNodes | LayoutTreeNode[] | Nodes in the layout tree |
| computedStyles | ComputedStyle[] | Whitelisted computed style properties for layout tree nodes |

### DOMStorage

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents browser DOM storage
(localStorage or sessionStorage) accessible via the Storage API.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| storageId | StorageId | Security origin and storage key identifier |
| entries | Item[] | Array of key-value pairs [key, value] |

### StorageId

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Identifier for DOM storage area,
combining security origin with storage key.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| securityOrigin | string | Security origin for the storage |
| storageKey | string | Key by which DOM Storage identifies its CachedStorageAreas |
| isLocalStorage | boolean | Whether storage is local (not session) |

### DeviceAccess

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents a device selection request
for browser automation, enabling interaction with connected physical devices.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| requestId | RequestId | Unique identifier for the device request |
| devices | PromptDevice[] | Available devices to choose from |

### PromptDevice

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Information about a device available for
selection in a browser device request prompt.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| id | DeviceId | Unique device identifier |
| name | string | Display name as shown in device prompt |

### DeviceOrientation

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Simulated device orientation for testing
orientation-dependent behavior.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| alpha | number | Rotation around z-axis (0-360 degrees) |
| beta | number | Front-to-back tilt (-180 to 180 degrees) |
| gamma | number | Left-to-right tilt (-90 to 90 degrees) |

### EmulatedEnvironment

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents various emulated environment
settings for browser testing including screen, sensors, user agent, and device posture.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| screenOrientation | ScreenOrientation | Emulated screen orientation |
| devicePosture | DevicePosture | Physical device posture (e.g., folded) |
| userAgentMetadata | UserAgentMetadata | User Agent Client Hints to emulate |
| screenInfo | ScreenInfo | Emulated screen properties |
| safeAreaInsets | SafeAreaInsets | Safe area insets for notched devices |

### ScreenOrientation

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Describes the orientation of the device
screen.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| type | string | Orientation type |
| angle | number | Orientation angle in degrees |

### ScreenInfo

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Describes screen properties similar to
window.getScreenDetails(), representing a display monitor.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| left | number | Offset of left screen edge |
| top | number | Offset of top screen edge |
| width | number | Screen width in pixels |
| height | number | Screen height in pixels |
| availLeft | number | Available area left offset |
| availTop | number | Available area top offset |
| availWidth | number | Available width excluding OS chrome |
| availHeight | number | Available height excluding OS chrome |
| devicePixelRatio | number | Device pixel ratio |
| orientation | ScreenOrientation | Screen orientation |
| colorDepth | number | Color depth in bits |
| isExtended | boolean | Has multiple screens |
| isInternal | boolean | Is internal display |
| isPrimary | boolean | Is primary screen |
| label | string | Descriptive label |
| id | ScreenId | Unique screen identifier |

### SensorReading

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents sensor data for emulating
device sensors in browser automation.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| single | SensorReadingSingle | Single value sensor (e.g., ambient light) |
| xyz | SensorReadingXYZ | 3-axis sensor data (e.g., accelerometer, gyroscope) |
| quaternion | SensorReadingQuaternion | 4-component orientation data |

### UserAgentMetadata

[PARTIAL SPEC — file too large to fully analyze (44 parts)] User Agent Client Hints for emulating
specific browser identities and platform characteristics.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| brands | UserAgentBrandVersion[] | Brands in Sec-CH-UA header |
| fullVersionList | UserAgentBrandVersion[] | Brands in Sec-CH-UA-Full-Version-List |
| fullVersion | string | Full version string |
| platform | string | Platform name (e.g., Windows, macOS) |
| platformVersion | string | Platform version |
| architecture | string | CPU architecture |
| model | string | Device model |
| mobile | boolean | Is mobile device |
| bitness | string | Architecture bitness |
| wow64 | boolean | Is Windows on Windows 64-bit |
| formFactors | string[] | Form factor values for Sec-CH-UA |

### FetchRequest

[PARTIAL SPEC — file too large to fully analyze (44 parts)] An HTTP request that has been
intercepted and paused by the Fetch domain, awaiting client response

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| requestId | RequestId | Unique identifier for this intercepted request |
| request | Network.Request | Details of the HTTP request including URL, method, headers |
| frameId | Page.FrameId | ID of the frame that initiated the request |
| resourceType | Network.ResourceType | Type of resource being requested (document, script, image, etc.) |
| responseErrorReason | Network.ErrorReason | Error if intercepted at response stage |
| responseStatusCode | number | HTTP status code if intercepted at response stage |
| responseStatusText | string | HTTP status text if intercepted at response stage |
| responseHeaders | HeaderEntry[] | Response headers if intercepted at response stage |
| networkId | Network.RequestId | Links to corresponding requestWillBeSent event |
| redirectedRequestId | RequestId | ID of request that caused redirect if this is a redirect result |

### AuthChallenge

[PARTIAL SPEC — file too large to fully analyze (44 parts)] An HTTP authentication challenge
encountered during request interception

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| requestId | RequestId | Unique identifier for the request requiring auth |
| request | Network.Request | The request details |
| frameId | Page.FrameId | ID of the frame that initiated the request |
| resourceType | Network.ResourceType | Type of resource being requested |
| authChallenge | AuthChallenge | Details of the authentication challenge (type, realm, etc.) |

### RequestPattern

[PARTIAL SPEC — file too large to fully analyze (44 parts)] A filter pattern to determine which
requests should be intercepted

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| urlPattern | string | Glob or regex pattern to match request URL |
| resourceType | Network.ResourceType[] | Array of resource types to match |
| requestMethod | string | HTTP method to match (GET, POST, etc.) |

### FulfilledResponse

[PARTIAL SPEC — file too large to fully analyze (44 parts)] A response provided by the client to
fulfill an intercepted request

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| requestId | RequestId | ID of request to fulfill |
| responseCode | number | HTTP response code to return |
| responseHeaders | HeaderEntry[] | Response headers to send |
| binaryResponseHeaders | binary | Headers as null-separated name:value pairs for non-UTF8 values |
| body | binary | Response body; uses original if absent at response stage, empty if at request stage |
| responsePhrase | string | Custom status text instead of standard phrase |

**Relationships:**

- responds-to FetchRequest (Fulfills a previously paused FetchRequest)

### RequestContinuation

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Modification parameters to continue a
paused request with changes

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| requestId | RequestId | ID of request to continue |
| url | string | Modified URL (not observable by page) |
| method | string | Overridden HTTP method |
| postData | binary | Overridden post data |
| headers | HeaderEntry[] | Overridden request headers |
| interceptResponse | boolean | Whether to intercept the response stage |

**Relationships:**

- continues FetchRequest (Continues a previously paused FetchRequest)

### AuthChallengeResponse

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Credentials provided to satisfy an
authentication challenge

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| requestId | RequestId | ID of request requiring auth |
| authChallengeResponse | AuthChallengeResponse | Response to auth challenge (credentials or cancel) |

**Relationships:**

- satisfies AuthChallenge (Provides credentials for an AuthChallenge)

### ResponseBody

[PARTIAL SPEC — file too large to fully analyze (44 parts)] The body content of an intercepted
response

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| requestId | RequestId | ID of intercepted request |
| body | string | Response body content |
| base64Encoded | boolean | Whether content is base64 encoded |

**Relationships:**

- reads-body-of FetchRequest (Reads response body from an intercepted FetchRequest)

### File

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents a file in a filesystem.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| name | string | File name |
| lastModified | Network.TimeSinceEpoch | Timestamp of last modification |
| size | number | Size in bytes |
| type | string | MIME type of the file |

### Directory

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents a directory in a filesystem
containing nested files and subdirectories.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| name | string | Directory name |
| nestedDirectories | string[] | Array of nested directory names |
| nestedFiles | File[] | Files that are directly nested under this directory |

**Relationships:**

- contains File (Directory contains nested files)

### BucketFileSystemLocator

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Locator for a file system within a
Storage Bucket (Storage API).

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| storageKey | Storage.SerializedStorageKey | Storage key |
| bucketName | string | Bucket name - defaults to default Bucket if not provided |
| pathComponents | string[] | Path to the directory using each path component as an array item |

### ScreenshotParams

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Encoding options for a screenshot in
headless mode.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| format | jpeg|png|webp | Image compression format (defaults to png) |
| quality | number | Compression quality from range [0..100] (jpeg and webp only) |
| optimizeForSpeed | boolean | Optimize image encoding for speed, not for resulting size (defaults to false) |

### DatabaseWithObjectStores

[PARTIAL SPEC — file too large to fully analyze (44 parts)] An IndexedDB database containing an
array of object stores.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| name | string | Database name |
| version | number | Database version (unsigned long long) |
| objectStores | ObjectStore[] | Object stores in this database |

**Relationships:**

- contains ObjectStore (Database contains object stores)

### ObjectStore

[PARTIAL SPEC — file too large to fully analyze (44 parts)] An IndexedDB object store that holds
records.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| name | string | Object store name |
| keyPath | KeyPath | Object store key path |
| autoIncrement | boolean | If true, object store has auto increment flag set |
| indexes | ObjectStoreIndex[] | Indexes in this object store |

**Relationships:**

- has-many ObjectStoreIndex (Object store has indexes)

### ObjectStoreIndex

[PARTIAL SPEC — file too large to fully analyze (44 parts)] An index in an IndexedDB object store
for querying records.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| name | string | Index name |
| keyPath | KeyPath | Index key path |
| unique | boolean | If true, index is unique |
| multiEntry | boolean | If true, index allows multiple entries for a key |

### Key

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents a key value in IndexedDB.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| type | number|string|date|array | Key type |
| number | number | Number value |
| string | string | String value |
| date | number | Date value |
| array | Key[] | Array value |

### KeyRange

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents a range of keys for IndexedDB
queries.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| lower | Key | Lower bound |
| upper | Key | Upper bound |
| lowerOpen | boolean | If true lower bound is open |
| upperOpen | boolean | If true upper bound is open |

### KeyPath

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Defines the path to access a key in an
object store.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| type | null|string|array | Key path type |
| string | string | String value |
| array | string[] | Array value |

### IndexedDBDatabase

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents an IndexedDB database in the
browser's storage.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| databaseName | string | Name of the database |
| objectStores | ObjectStore[] | Array of object stores contained in the database |

**Relationships:**

- contains ObjectStore (Database contains multiple object stores)

### TouchPoint

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents a touch input point for
gesture interactions.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| x | number | X coordinate relative to viewport in CSS pixels |
| y | number | Y coordinate relative to viewport in CSS pixels |
| radiusX | number | X radius of touch area (default: 1.0) |
| radiusY | number | Y radius of touch area (default: 1.0) |
| rotationAngle | number | Rotation angle in degrees (default: 0.0) |
| force | number | Force of touch (default: 1.0) |
| tangentialPressure | number | Normalized tangential pressure [-1,1] (default: 0) |
| tiltX | number | Plane angle between Y-Z plane and stylus plane, degrees [-90,90] |
| tiltY | number | Plane angle between X-Z plane and stylus plane, degrees [-90,90] |
| twist | number | Clockwise rotation of pen stylus, degrees [0-359] |
| id | number | Identifier to track touch sources between events |

### DragData

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents data being dragged during
drag-and-drop operations.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| items | DragDataItem[] | Array of drag data items |
| files | string[] | List of filenames to include when dropping |
| dragOperationsMask | number | Bit field for allowed operations: Copy=1, Link=2, Move=16 |

**Relationships:**

- contains DragDataItem (DragData contains multiple drag items)

### DragDataItem

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents a single item of data being
dragged.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| mimeType | string | MIME type of the dragged data |
| data | string | The dragged data content (link, text, HTML, etc.) |
| title | string | Title associated with a link (valid for text/uri-list) |
| baseURL | string | Base URL for contained markup (valid for text/html) |

**Relationships:**

- belongs to DragData (Item belongs to a drag operation)

### NetworkRequest

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents an HTTP network request made
by the browser, including URL, method, headers, and timing information.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| url | string | Request URL without fragment |
| urlFragment | string | Fragment of URL starting with hash if present |
| method | string | HTTP request method |
| headers | Headers | HTTP request headers as key-value pairs |
| postData | string | HTTP POST request data (deprecated, use postDataEntries) |
| hasPostData | boolean | True when request has POST data |
| initialPriority | ResourcePriority | Priority of resource request at time of sending |
| referrerPolicy | string | Referrer policy as defined in W3C spec |
| isSameSite | boolean | True if resource is same-site as main frame |
| isAdRelated | boolean | True when resource request is ad-related |

### NetworkResponse

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents an HTTP response received by
the browser, including status, headers, timing, and security details.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| url | string | Response URL |
| status | number | HTTP response status code |
| statusText | string | HTTP response status text |
| headers | Headers | HTTP response headers |
| mimeType | string | Resource mimeType as determined by browser |
| charset | string | Resource charset if applicable |
| fromDiskCache | boolean | Request served from disk cache |
| fromServiceWorker | boolean | Request served from ServiceWorker |
| encodedDataLength | number | Total bytes received for request |
| timing | ResourceTiming | Timing information for request |
| securityState | Security.SecurityState | Security state of request resource |
| securityDetails | SecurityDetails | Security details for the request |

### Cookie

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents a browser cookie with all its
attributes including name, value, domain, path, and security settings.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| name | string | Cookie name |
| value | string | Cookie value |
| domain | string | Cookie domain |
| path | string | Cookie path |
| expires | number | Expiration date as seconds since UNIX epoch, -1 for session |
| size | number | Cookie size in bytes |
| httpOnly | boolean | True if cookie is http-only |
| secure | boolean | True if cookie is secure |
| session | boolean | True if session cookie |
| sameSite | CookieSameSite | Cookie SameSite type |
| priority | CookiePriority | Cookie priority |
| sameParty | boolean | True if cookie is SameParty |
| sourceScheme | CookieSourceScheme | Source scheme of cookie origin |
| sourcePort | number | Cookie source port |
| partitionKey | CookiePartitionKey | Cookie partition key |

### ResourceTiming

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Timing information for a network
request, capturing latency metrics for each phase of the request lifecycle.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| requestTime | number | Baseline in seconds for other timing values |
| proxyStart | number | Started resolving proxy (ms) |
| proxyEnd | number | Finished resolving proxy (ms) |
| dnsStart | number | Started DNS address resolve (ms) |
| dnsEnd | number | Finished DNS address resolve (ms) |
| connectStart | number | Started connecting to remote host (ms) |
| connectEnd | number | Connected to remote host (ms) |
| sslStart | number | Started SSL handshake (ms) |
| sslEnd | number | Finished SSL handshake (ms) |
| workerStart | number | Started running ServiceWorker (ms) |
| sendStart | number | Started sending request (ms) |
| sendEnd | number | Finished sending request (ms) |
| receiveHeadersStart | number | Started receiving response headers (ms) |
| receiveHeadersEnd | number | Finished receiving response headers (ms) |

### SecurityDetails

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Security information about a network
request including TLS protocol, cipher, certificate details, and Certificate Transparency
compliance.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| protocol | string | Protocol name (e.g. TLS 1.2, QUIC) |
| keyExchange | string | Key Exchange used by connection |
| keyExchangeGroup | string | (EC)DH group used by connection if applicable |
| cipher | string | Cipher name |
| mac | string | TLS MAC (omitted for AEAD ciphers) |
| certificateId | Security.CertificateId | Certificate ID value |
| subjectName | string | Certificate subject name |
| sanList | string[] | Subject Alternative Name DNS names and IPs |
| issuer | string | Name of issuing CA |
| validFrom | TimeSinceEpoch | Certificate valid from date |
| validTo | TimeSinceEpoch | Certificate valid to (expiration) date |
| signedCertificateTimestampList | SignedCertificateTimestamp[] | List of signed certificate timestamps |
| certificateTransparencyCompliance | CertificateTransparencyCompliance | CT policy compliance status |
| encryptedClientHello | boolean | Whether connection used Encrypted ClientHello |

### CachedResource

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Information about a resource stored in
the browser cache.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| url | string | Resource URL of original network request |
| type | ResourceType | Type of resource |
| response | Response | Cached response data |
| bodySize | number | Cached response body size |

### Initiator

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Information about what triggered a
network request, including type and optional stack trace.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| type | string | Type of initiator (parser, script, preload, etc.) |
| stack | Runtime.StackTrace | JavaScript stack trace (for Script type) |
| url | string | Initiator URL for Parser/Script/SignedExchange types |
| lineNumber | number | Initiator line number (0-based) |
| columnNumber | number | Initiator column number (0-based) |
| requestId | RequestId | Request ID that triggered this request |

### WebSocketFrame

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents a WebSocket message frame
with opcode, mask, and payload data.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| opcode | number | WebSocket message opcode |
| mask | boolean | WebSocket message mask |
| payloadData | string | UTF-8 string for text, base64 for binary |

### HighlightConfig

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Configuration descriptor for the visual
appearance of element highlights and overlays during inspection mode

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| showInfo | boolean | Whether to show element info |
| showStyles | boolean | Whether to show computed styles |
| showAccessibilityInfo | boolean | Whether to show accessibility info |
| showMetrics | boolean | Whether to show metrics |
| contentColor | DOM.RGBA | Content box highlight fill color |
| contentOutlineColor | DOM.RGBA | Content box highlight outline color |
| paddingColor | DOM.RGBA | Padding box highlight fill color |
| borderColor | DOM.RGBA | Border box highlight fill color |
| marginColor | DOM.RGBA | Margin box highlight fill color |
| eventTargetColor | DOM.RGBA | Event target highlight color |
| shapeColor | DOM.RGBA | Shape outside fill color |
| shapeMarginColor | DOM.RGBA | Shape margin fill color |
| cspInfoColor | DOM.RGBA | CSP violation highlight color |
| gridHighlightConfig | object | Grid layout highlight configuration |
| flexHighlightConfig | object | Flex layout highlight configuration |
| isolationModeHighlightConfig | object | Isolation mode highlight configuration |
| resizerHandleColor | DOM.RGBA | Fill color for resizer handles |
| maskColor | DOM.RGBA | Fill color for mask covering non-isolated elements |

### InspectMode

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents the current inspection mode
that determines how elements are highlighted and what actions trigger on user interaction

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| mode | string | The inspection mode: searchForNode, searchForUAShadowDOM, captureAreaScreenshot, or none |

### InspectNodeRequest

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Event payload fired when a DOM node
should be inspected, typically triggered by user clicking an element in inspect mode

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| backendNodeId | DOM.BackendNodeId | Unique identifier of the node to inspect |

### NodeHighlightRequest

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Event payload fired when a DOM node
should be visually highlighted in the browser viewport

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| nodeId | DOM.NodeId | Identifier of the node to highlight |

### ScreenshotRequest

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Event payload fired when user requests
to capture a screenshot of a specific area on the page

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| viewport | Page.Viewport | Viewport to capture in device independent pixels |

### GridOverlay

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Visual overlay configuration for
highlighting CSS Grid elements in the page

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| gridNodeHighlightConfigs | GridNodeHighlightConfig[] | Array of node identifiers and highlight appearance descriptors |

### FlexOverlay

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Visual overlay configuration for
highlighting CSS Flexbox elements in the page

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| flexNodeHighlightConfigs | FlexNodeHighlightConfig[] | Array of node identifiers and highlight appearance descriptors |

### ScrollSnapOverlay

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Visual overlay configuration for
highlighting scroll snap regions in the page

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| scrollSnapHighlightConfigs | ScrollSnapHighlightConfig[] | Array of node identifiers and highlight appearance descriptors |

### ContainerQueryOverlay

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Visual overlay configuration for
highlighting container query elements in the page

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| containerQueryHighlightConfigs | ContainerQueryHighlightConfig[] | Array of node identifiers and highlight appearance descriptors |

### IsolatedElementOverlay

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Visual overlay configuration for showing
elements in isolation mode, where non-isolated elements are masked

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| isolatedElementHighlightConfigs | IsolatedElementHighlightConfig[] | Array of node identifiers and highlight appearance descriptors |

### WindowControlsOverlay

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Visual overlay configuration for
displaying window controls on Progressive Web Apps

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| windowControlsOverlayConfig | WindowControlsOverlayConfig | Window Controls Overlay data, null to hide |

### Hinge

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Visual overlay configuration for
displaying a dual-screen device hinge

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| hingeConfig | HingeConfig | Hinge data configuration, null to hide |

### HighlightQuad

[PARTIAL SPEC — file too large to fully analyze (44 parts)] A quad (four-point polygon) to be
highlighted on the page with absolute coordinates relative to the main frame viewport

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| quad | DOM.Quad | Four points defining the quad to highlight |
| color | DOM.RGBA | Highlight fill color |
| outlineColor | DOM.RGBA | Highlight outline color |

### HighlightRect

[PARTIAL SPEC — file too large to fully analyze (44 parts)] A rectangle region to be highlighted on
the page with coordinates relative to the main frame viewport

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| x | number | X coordinate |
| y | number | Y coordinate |
| width | number | Rectangle width |
| height | number | Rectangle height |
| color | DOM.RGBA | Highlight fill color |
| outlineColor | DOM.RGBA | Highlight outline color |

### FrameHighlight

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Visual highlight for a frame element in
the page

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| frameId | Page.FrameId | Identifier of the frame to highlight |
| contentColor | DOM.RGBA | Content box highlight fill color |
| contentOutlineColor | DOM.RGBA | Content box highlight outline color |

### DebugSettings

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Collection of debug visualization
settings for browser internals

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| showDebugBorders | boolean | Whether to show debug borders on layers |
| showFPSCounter | boolean | Whether to show the FPS counter |
| showPaintRects | boolean | Whether to show paint rectangles |
| showLayoutShiftRegions | boolean | Whether to show layout shift regions |
| showScrollBottleneckRects | boolean | Whether to show scroll bottleneck rects |
| showAdHighlights | boolean | Whether to show highlights on ad frames |
| showViewportSizeOnResize | boolean | Whether to paint viewport size on resize |
| showWebVitals | boolean | Whether to show Web Vitals indicators |

### HighlightTestData

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Test data structure for retrieving
highlight information about a DOM node

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| nodeId | DOM.NodeId | Id of the node to get highlight object for |
| includeDistance | boolean | Whether to include distance info |
| includeStyle | boolean | Whether to include style info |
| colorFormat | ColorFormat | Color format for the config |
| showAccessibilityInfo | boolean | Whether to show accessibility info |

### GridTestData

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Test data structure for retrieving CSS
Grid highlight information for multiple nodes

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| nodeIds | DOM.NodeId[] | Array of node IDs to get grid highlight objects for |

### SourceOrderTestData

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Test data structure for retrieving
source order viewer highlight information for a DOM node

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| nodeId | DOM.NodeId | Id of the node to highlight |

### Frame

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents a browser frame (iframe or
main frame) on a page with its security, loading, and content metadata.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| id | FrameId | Unique frame identifier |
| parentId | FrameId | Parent frame identifier for nested frames |
| loaderId | Network.LoaderId | Identifier of the loader associated with this frame |
| name | string | Frame's name as specified in the iframe tag |
| url | string | Frame document's URL without fragment |
| urlFragment | string | Frame document's URL fragment including the '#' |
| domainAndRegistry | string | Frame document's registered domain |
| securityOrigin | string | Frame document's security origin |
| securityOriginDetails | SecurityOriginDetails | Additional details about security origin |
| mimeType | string | Frame document's mimeType as determined by the browser |
| unreachableUrl | string | URL that could not be loaded if frame failed |
| adFrameStatus | AdFrameStatus | Indicates whether frame was tagged as an ad |
| secureContextType | SecureContextType | Whether the main document is a secure context |
| crossOriginIsolatedContextType | CrossOriginIsolatedContextType | Whether this is a cross origin isolated context |
| gatedAPIFeatures | GatedAPIFeatures[] | Which gated APIs/features are available |

**Relationships:**

- has-parent Frame (Frames can be nested with parent/child relationships)
- contains FrameResource (Frame contains resources)

### WebAppManifest

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents a Progressive Web App
manifest defining app metadata, display settings, file handlers, and capabilities.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| id | string | Unique identifier for the web app |
| name | string | Name of the web app |
| shortName | string | Short name for the web app |
| startUrl | string | Start URL of the web app |
| display | string | Display mode (fullscreen, standalone, etc.) |
| displayOverrides | string[] | User-controlled display mode overrides |
| backgroundColor | string | Background color |
| themeColor | string | Theme color |
| description | string | Description of the web app |
| dir | string | Text direction |
| lang | string | Primary language |
| icons | ImageResource[] | App icons |
| fileHandlers | FileHandler[] | Handlers to open files |
| protocolHandlers | ProtocolHandler[] | Handlers to open protocols |
| scope | string | Scope of the web app |
| scopeExtensions | ScopeExtension[] | Non-standard scope extensions |
| orientation | string | Screen orientation |
| preferRelatedApplications | boolean | Whether to prefer related applications |
| relatedApplications | RelatedApplication[] | Related applications |
| launchHandler | LaunchHandler | Launch handler configuration |
| screenshots | Screenshot[] | Screenshots for the app |
| shareTarget | ShareTarget | Share target configuration |
| shortcuts | Shortcut[] | App shortcuts |

**Relationships:**

- has-many FileHandler (Manifest can define multiple file handlers)
- has-many ProtocolHandler (Manifest can define multiple protocol handlers)

### NavigationEntry

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents a navigation history entry in
the browser's session history.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| id | number | Unique id of the navigation history entry |
| url | string | URL of the navigation history entry |
| userTypedURL | string | URL that the user typed in the url bar |
| title | string | Title of the navigation history entry |
| transitionType | TransitionType | How the navigation was triggered |

### Viewport

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents a viewport configuration for
capturing screenshots or performing visual operations.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| x | number | X offset in device independent pixels |
| y | number | Y offset in device independent pixels |
| width | number | Rectangle width in device independent pixels |
| height | number | Rectangle height in device independent pixels |
| scale | number | Page scale factor |

### FrameResource

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents a resource (image, script,
stylesheet, etc.) loaded within a frame.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| url | string | Resource URL |
| type | Network.ResourceType | Type of this resource |
| mimeType | string | Resource mimeType as determined by the browser |
| lastModified | Network.TimeSinceEpoch | Last-modified timestamp as reported by server |
| contentSize | number | Resource content size |
| failed | boolean | True if the resource failed to load |
| canceled | boolean | True if the resource was canceled during loading |

**Relationships:**

- belongs to Frame (Resource belongs to a frame)

### PWA

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents a Progressive Web App with
its installation state, file handlers, and badge count.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| manifestId | string | The id from the webapp's manifest file, commonly the URL of the site |
| badgeCount | number | Current badge count for the app |
| fileHandlers | FileHandler[] | Registered file handlers for the app |

### FileHandler

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Defines a file handler that allows a PWA
to open specific file types.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| action | string | URL action for handling files |
| accepts | FileHandlerAccept[] | Accepted file types with mimetypes and extensions |
| displayName | string | Human-readable display name |

### InstallabilityError

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents an error that prevents a web
app from being installable as a PWA.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| errorId | string | The error identifier (e.g., 'manifest-missing-suitable-icon') |
| errorArguments | InstallabilityErrorArgument[] | List of error arguments with name-value pairs |

### FileChooserEvent

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents a file chooser dialog opening
event in the browser

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| frameId | FrameId | Id of the frame containing input node |
| mode | "selectSingle"|"selectMultiple" | Input mode |
| backendNodeId | DOM.BackendNodeId | Input node id for file choosers opened via input element |

### PreloadingAttemptKey

[PARTIAL SPEC — file too large to fully analyze (44 parts)] A key that identifies a preloading
attempt (prefetch or prerender) in Chrome's speculation rules implementation

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| loaderId | Network.LoaderId | Unique identifier for the network loader |
| action | SpeculationAction | Type of preloading action being performed |
| url | string | The URL specified by the trigger (initial URL, not final URL after redirects) |
| targetHint | SpeculationTargetHint | Target hint for window name targeting |

### PreloadingAttemptSource

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Identifies the sources that triggered a
preloading attempt, including rule sets and DOM elements

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| key | PreloadingAttemptKey | The key identifying this preloading attempt |
| ruleSetIds | RuleSetId[] | IDs of rule sets that had a speculation rule triggering this attempt |
| nodeIds | DOM.BackendNodeId[] | BackendNodeIds of anchor elements that triggered the attempt |

**Relationships:**

- references PreloadingAttemptKey (References the attempt key)

### PreloadingStatus

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Current status of a preloading attempt
(prefetch or prerender)

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| value | string | Status: Pending, Running, Ready, Success, Failure, or NotSupported |

### PrefetchStatus

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Detailed status information about a
prefetch attempt, including eligibility and success conditions

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| value | string | Prefetch status enum covering eligibility, failures, and usage |

### PrerenderFinalStatus

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Final status reasons for Prerender2
attempts, indicating why prerendering succeeded or was cancelled

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| value | string | Status explaining the final outcome of a prerender attempt |

### PrerenderMismatchedHeaders

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Information about HTTP headers that
mismatched between initial prerender and activation

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| headerName | string | Name of the mismatched header |
| initialValue | string | Header value during prerender phase |
| activationValue | string | Header value during activation phase |

### SecurityState

[PARTIAL SPEC — file too large to fully analyze (44 parts)] The security level of a page or resource

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| value | string | Security level: unknown, neutral, insecure, secure, info, or insecure-broken |

### CertificateSecurityState

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Details about the security state of the
page certificate including protocol, cipher, and validity

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| protocol | string | Protocol name (e.g., TLS 1.2 or QUIC) |
| keyExchange | string | Key Exchange used by the connection |
| keyExchangeGroup | string | (EC)DH group used by the connection |
| cipher | string | Cipher name |
| mac | string | TLS MAC (not present for AEAD ciphers) |
| certificate | string[] | Page certificate |
| subjectName | string | Certificate subject name |
| issuer | string | Name of the issuing CA |
| validFrom | Network.TimeSinceEpoch | Certificate valid from date |
| validTo | Network.TimeSinceEpoch | Certificate valid to (expiration) date |
| certificateNetworkError | string | Highest priority network error code if certificate has an error |
| certificateHasWeakSignature | boolean | True if certificate uses weak signature algorithm |
| certificateHasSha1Signature | boolean | True if certificate has SHA1 signature in the chain |
| modernSSL | boolean | True if using modern SSL |
| obsoleteSslProtocol | boolean | True if using obsolete SSL protocol |
| obsoleteSslKeyExchange | boolean | True if using obsolete SSL key exchange |
| obsoleteSslCipher | boolean | True if using obsolete SSL cipher |
| obsoleteSslSignature | boolean | True if using obsolete SSL signature |

### SafetyTipInfo

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Information about safety tips or
reputation warnings triggered on a page

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| safetyTipStatus | SafetyTipStatus | Type of safety tip or reputation warning triggered |
| safeUrl | string | URL suggested by safety tip (for lookalike matches) |

### VisibleSecurityState

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Current security state information
visible to the user about the page

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| securityState | SecurityState | The security level of the page |
| certificateSecurityState | CertificateSecurityState | Security state details about the page certificate |
| safetyTipInfo | SafetyTipInfo | Type of Safety Tip triggered on the page |
| securityStateIssueIds | string[] | Array of security state issue IDs |

**Relationships:**

- has CertificateSecurityState (Contains certificate security details)
- has SafetyTipInfo (Contains safety tip information)

### SecurityStateExplanation

[PARTIAL SPEC — file too large to fully analyze (44 parts)] An explanation of a factor contributing
to the security state of a page

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| securityState | SecurityState | Security state representing the severity of the factor |
| title | string | Title describing the type of factor |
| summary | string | Short phrase describing the type of factor |
| description | string | Full text explanation of the factor |
| mixedContentType | MixedContentType | Type of mixed content described |
| certificate | string[] | Page certificate |
| recommendations | string[] | Recommendations to fix any issues |

### InsecureContentStatus

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Information about insecure content (HTTP
resources) on HTTPS pages

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| ranMixedContent | boolean | Always false in current implementation |
| displayedMixedContent | boolean | Always false in current implementation |
| containedMixedForm | boolean | Always false in current implementation |
| ranContentWithCertErrors | boolean | Always false in current implementation |
| displayedContentWithCertErrors | boolean | Always false in current implementation |
| ranInsecureContentStyle | SecurityState | Always set to unknown |
| displayedInsecureContentStyle | SecurityState | Always set to unknown |

### PreloadEnabledState

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Configuration state of browser
preloading features, indicating what disabled them

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| disabledByPreference | boolean | Disabled by user preference |
| disabledByDataSaver | boolean | Disabled by Data Saver mode |
| disabledByBatterySaver | boolean | Disabled by Battery Saver mode |
| disabledByHoldbackPrefetchSpeculationRules | boolean | Prefetch held back by Chrome for testing |
| disabledByHoldbackPrerenderSpeculationRules | boolean | Prerender held back by Chrome for testing |

### ServiceWorkerRegistration

[PARTIAL SPEC — file too large to fully analyze (44 parts)] ServiceWorker registration representing
a registered service worker in the browser.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| registrationId | string | Unique identifier for this registration |
| scopeURL | string | The URL scope that the service worker controls |
| isDeleted | boolean | Whether this registration has been deleted |

### ServiceWorkerVersion

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents an active instance of a
service worker with its runtime state.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| versionId | string | Unique identifier for this version |
| registrationId | string | ID of the parent registration |
| scriptURL | string | URL of the service worker script |
| runningStatus | string | Current running state: stopped|starting|running|stopping |
| status | string | Lifecycle status: new|installing|installed|activating|activated|redundant |
| scriptLastModified | number | Last-Modified header of main script |
| scriptResponseTime | number | Time when response headers were received |
| controlledClients | TargetID[] | Clients controlled by this worker |
| targetId | TargetID | Associated target ID |
| routerRules | string | Router rules for this worker |

**Relationships:**

- belongs to ServiceWorkerRegistration (Version belongs to a registration)

### ServiceWorkerErrorMessage

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Error information reported by a service
worker.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| errorMessage | string | The error message text |
| registrationId | string | ID of the registration that produced this error |
| versionId | string | ID of the worker version that produced this error |
| sourceURL | string | URL of the script where the error occurred |
| lineNumber | number | Line number of the error |
| columnNumber | number | Column number of the error |

### StorageBucket

[PARTIAL SPEC — file too large to fully analyze (44 parts)] A storage bucket container for storing
browser data with quota and expiration.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| storageKey | string | The storage key identifying this bucket |
| name | string | Optional bucket name, defaults to default bucket |

### StorageBucketInfo

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Detailed information about a storage
bucket including quota, expiration and durability settings.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| bucket | StorageBucket | The bucket definition |
| id | string | Unique identifier |
| expiration | number | Expiration timestamp (TimeSinceEpoch) |
| quota | number | Storage quota in bytes |
| persistent | boolean | Whether storage persists across sessions |
| durability | string | Durability setting: relaxed or strict |

**Relationships:**

- contains StorageBucket (Contains bucket metadata)

### SharedStorageEntry

[PARTIAL SPEC — file too large to fully analyze (44 parts)] A key-value pair stored in the browser's
shared storage API.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| key | string | The storage key |
| value | string | The stored value |

### SharedStorageMetadata

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Metadata about an origin's shared
storage including usage statistics and budget.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| creationTime | number | Time when shared storage was created |
| length | number | Number of key-value pairs stored |
| remainingBudget | number | Bits of entropy remaining in navigation budget |
| bytesUsed | number | Total bytes stored as key-value pairs |

### AttributionReportingSourceRegistration

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Attribution reporting source
configuration for privacy-preserving ad conversion tracking.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| time | number | Registration time |
| expiry | number | Duration in seconds before source expires |
| triggerData | number[] | Available trigger data values |
| eventReportWindows | AttributionReportingEventReportWindows | Windows for event-level reports |
| aggregatableReportWindow | number | Duration in seconds for aggregatable report |
| type | string | Source type: navigation or event |
| sourceOrigin | string | Origin of the source |
| reportingOrigin | string | Origin for reporting |
| destinationSites | string[] | Sites where attribution can occur |
| eventId | string | Unique event identifier (unsigned int64) |
| priority | string | Source priority (signed int64) |
| filterData | AttributionReportingFilterDataEntry[] | Filter configuration for matching |
| aggregationKeys | AttributionReportingAggregationKeysEntry[] | Keys for aggregatable reports |
| debugKey | string | Optional debug key |
| triggerDataMatching | string | Matching mode: exact or modulus |
| destinationLimitPriority | string | Priority for destination limit |
| aggregatableDebugReportingConfig | AttributionReportingAggregatableDebugReportingConfig | Debug reporting configuration |
| maxEventLevelReports | number | Maximum number of event-level reports |
| namedBudgets | AttributionReportingNamedBudgetDef[] | Named budget allocations |
| debugReporting | boolean | Whether debug reporting is enabled |
| eventLevelEpsilon | number | Privacy epsilon for event-level reports |

### AttributionReportingTriggerRegistration

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Trigger registration for attribution
reporting - represents a conversion/event to be attributed.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| filters | AttributionReportingFilterPair | Filter configuration |
| debugKey | string | Optional debug key |
| aggregatableDedupKeys | AttributionReportingAggregatableDedupKey[] | Deduplication keys for aggregatable reports |
| eventTriggerData | AttributionReportingEventTriggerData[] | Event-level trigger data |
| aggregatableTriggerData | AttributionReportingAggregatableTriggerData[] | Aggregatable trigger data |
| aggregatableValues | AttributionReportingAggregatableValueEntry[] | Values for aggregatable reports |
| aggregatableFilteringIdMaxBytes | number | Max bytes for filtering IDs |
| debugReporting | boolean | Whether debug reporting enabled |
| aggregationCoordinatorOrigin | string | Aggregation coordinator origin |
| sourceRegistrationTimeConfig | string | Include or exclude source registration time |
| triggerContextId | string | Trigger context ID |
| aggregatableDebugReportingConfig | AttributionReportingAggregatableDebugReportingConfig | Debug reporting config |
| scopes | string[] | Attribution scopes |
| namedBudgets | AttributionReportingNamedBudgetCandidate[] | Named budget candidates |

### RelatedWebsiteSet

[PARTIAL SPEC — file too large to fully analyze (44 parts)] A Related Website Set (formerly
First-Party Set) grouping related sites for privacy purposes.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| primarySites | string[] | Primary sites of this set including ccTLDs |
| associatedSites | string[] | Associated sites of this set including ccTLDs |
| serviceSites | string[] | Service sites of this set including ccTLDs |

### TrustTokens

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Trust Tokens storage information -
represents signed but unused tokens from an issuer.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| issuerOrigin | string | Origin of the token issuer |
| count | number | Number of available tokens |

### UsageForType

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Storage usage information for a specific
storage type.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| storageType | string | Type of storage (cookies, local_storage, etc.) |
| usage | number | Storage usage in bytes |

### CacheStorage

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Browser Cache Storage API - manages HTTP
caches for origin-specific resources

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| origin | string | Security origin for the cache storage |
| storageKey | string | Storage key identifying the storage partition |
| bucketId | string | Storage bucket identifier |
| cacheName | string | Name of the cache in the origin |
| associatedSites | string[] | Associated sites with ccTLDs |
| serviceSites | string[] | Service sites with ccTLDs |

### IndexedDB

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Browser IndexedDB object store -
client-side structured data storage

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| origin | string | Security origin for the database |
| storageKey | string | Storage key for partitioning |
| bucketId | string | Storage bucket identifier |
| databaseName | string | Name of the IndexedDB database |
| objectStoreName | string | Name of the object store within the database |

### SharedStorage

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Privacy Sandbox Shared Storage API -
cross-site storage for advertising use cases

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| ownerOrigin | string | Origin owning the shared storage data |
| ownerSite | string | Site owning the shared storage data |
| accessTime | Network.TimeSinceEpoch | Time of the storage access |
| scope | SharedStorageAccessScope | Enum indicating access scope |
| method | SharedStorageAccessMethod | Shared Storage API method invoked |
| mainFrameId | Page.FrameId | DevTools Frame Token for primary frame |
| operationId | string | ID of the operation call |
| workletTargetId | Target.TargetID | DevTools token for associated worklet |
| executionTime | number | Execution time in microseconds |

**Relationships:**

- has-metadata SharedStorageMetadata (Contains metadata about shared storage)

### InterestGroup

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Privacy Sandbox Interest Group API -
advertising audience targeting for FLEDGE auctions

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| ownerOrigin | string | Origin owning the interest group |
| name | string | Name of the interest group |
| accessTime | Network.TimeSinceEpoch | Time the group was accessed |
| type | InterestGroupAccessType | Type of access event |
| componentSellerOrigin | string | For topLevelBid events |
| bid | number | Bid amount if bid event |
| bidCurrency | string | Currency of the bid |
| uniqueAuctionId | InterestGroupAuctionId | Unique auction identifier |

**Relationships:**

- participates-in InterestGroupAuction (Interest groups participate in auctions)

### InterestGroupAuction

[PARTIAL SPEC — file too large to fully analyze (44 parts)] FLEDGE auction involving interest groups
for programmatic advertising

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| uniqueAuctionId | InterestGroupAuctionId | Unique identifier for the auction |
| parentAuctionId | InterestGroupAuctionId | Parent auction for child auctions |
| eventTime | Network.TimeSinceEpoch | Time of the auction event |
| type | InterestGroupAuctionEventType | Type of auction event |
| auctionConfig | object | Auction configuration for started/configResolved events |

**Relationships:**

- uses InterestGroup (Auction uses interest groups for bidding)

### AttributionReport

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Privacy Sandbox Attribution Reporting
API - tracks ad conversion attribution

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| url | string | Report destination URL |
| body | object | Report payload as key-value pairs |
| result | AttributionReportingReportResult | Result status of the report |
| netError | number | Network error code if applicable |
| netErrorName | string | Network error name |
| httpStatusCode | number | HTTP response status code |

**Relationships:**

- generated-from AttributionReportingSourceRegistration (Reports generated from source registrations)

### TrustToken

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Privacy Sandbox Trust Token -
cryptographic tokens for cross-site privacy-preserving attribution

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| issuerOrigin | string | Origin that issued the tokens |
| count | number | Number of tokens held |

### StorageQuota

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Browser storage quota - tracks usage and
limits for origin-based storage

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| origin | string | Security origin |
| usage | number | Storage usage in bytes |
| quota | number | Storage quota in bytes |
| overrideActive | boolean | Whether quota override is active |
| usageBreakdown | UsageForType[] | Usage per storage type |

### GPUDevice

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Describes a single graphics processor
(GPU) on the system.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| vendorId | number | PCI ID of the GPU vendor, if available; 0 otherwise |
| deviceId | number | PCI ID of the GPU device, if available; 0 otherwise |
| subSysId | number | Sub sys ID of the GPU, only available on Windows |
| revision | number | Revision of the GPU, only available on Windows |
| vendorString | string | String description of the GPU vendor if PCI ID is not available |
| deviceString | string | String description of the GPU device if PCI ID is not available |
| driverVendor | string | String description of the GPU driver vendor |
| driverVersion | string | String description of the GPU driver version |

### Size

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Describes the width and height
dimensions of an entity, typically used for resolutions.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| width | number | Width in pixels |
| height | number | Height in pixels |

### VideoDecodeAcceleratorCapability

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Describes a supported video decoding
profile with its associated minimum and maximum resolutions.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| profile | string | Video codec profile that is supported, e.g. VP9 Profile 2 |
| maxResolution | Size | Maximum video dimensions in pixels supported for this profile |
| minResolution | Size | Minimum video dimensions in pixels supported for this profile |

**Relationships:**

- contains Size (Contains minResolution and maxResolution Size objects)

### VideoEncodeAcceleratorCapability

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Describes a supported video encoding
profile with its associated maximum resolution and maximum framerate.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| profile | string | Video codec profile that is supported, e.g. H264 Main |
| maxResolution | Size | Maximum video dimensions in pixels supported for this profile |
| maxFramerateNumerator | number | Maximum encoding framerate numerator in frames per second |
| maxFramerateDenominator | number | Maximum encoding framerate denominator |

**Relationships:**

- contains Size (Contains maxResolution Size object)

### GPUInfo

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Provides information about the GPU(s) on
the system including devices, capabilities, and driver workarounds.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| devices | GPUDevice[] | The graphics devices on the system. Element 0 is the primary GPU |
| auxAttributes | object | Optional dictionary of additional GPU related attributes |
| featureStatus | object | Optional dictionary of graphics features and their status |
| driverBugWorkarounds | string[] | Array of GPU driver bug workarounds |
| videoDecoding | VideoDecodeAcceleratorCapability[] | Supported accelerated video decoding capabilities |
| videoEncoding | VideoEncodeAcceleratorCapability[] | Supported accelerated video encoding capabilities |

**Relationships:**

- has-many GPUDevice (Contains multiple GPUDevice objects)
- has-many VideoDecodeAcceleratorCapability (Contains video decoding capabilities)
- has-many VideoEncodeAcceleratorCapability (Contains video encoding capabilities)

### ProcessInfo

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents information about a running
process in the browser.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| type | string | Specifies process type |
| id | number | Specifies process id |
| cpuTime | number | Cumulative CPU usage in seconds across all threads since process start |

### TargetInfo

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Information about a browser target such
as a page, iframe, or service worker.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| targetId | string | Unique identifier of the target |
| type | string | Type of target (page, iframe, service_worker, etc.) |
| title | string | Title of the target |
| url | string | URL of the target |
| attached | boolean | Whether the target has an attached client |
| openerId | string | Opener target Id |
| canAccessOpener | boolean | Whether the target has access to the originating window |
| openerFrameId | string | Frame id of originating window (only set if target has an opener) |
| parentFrameId | string | Id of the parent frame, only present for iframe targets |
| browserContextId | string | Browser context identifier |
| subtype | string | Additional details for specific target types (e.g., 'prerender' for page type) |

**Relationships:**

- belongs to BrowserContext (Target belongs to a browser context)

### FilterEntry

[PARTIAL SPEC — file too large to fully analyze (44 parts)] A filter used by target
query/discovery/auto-attach operations to include or exclude targets.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| exclude | boolean | If set, causes exclusion of matching targets from the list |
| type | string | If not present, matches any type |

### RemoteLocation

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents a remote connection location
with host and port.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| host | string | Hostname or IP address |
| port | number | Port number |

### Target

[PARTIAL SPEC — file too large to fully analyze (44 parts)] A browser inspection target - represents
a page, worker, service worker, browser context, or other inspectable entity in the browser.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| targetId | TargetID | Unique identifier for the target |
| type | string | Type of target (page, worker, service_worker, browser, etc.) |
| url | string | URL of the target if applicable |
| browserContextId | Browser.BrowserContextID | The browser context this target belongs to |

**Relationships:**

- belongs to BrowserContext (Target belongs to a browser context)
- has-many Session (Target can have multiple debugging sessions attached)

### Session

[PARTIAL SPEC — file too large to fully analyze (44 parts)] A debugging session used to send/receive
messages to a target via the Chrome DevTools Protocol.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| sessionId | SessionID | Identifier assigned to the session for sending/receiving messages |
| targetId | TargetID | The target this session is attached to |
| flatten | boolean | Whether to use flat access mode (sessionId in commands) |

**Relationships:**

- belongs to Target (Session is attached to a target)

### WindowState

[PARTIAL SPEC — file too large to fully analyze (44 parts)] The visual state of a browser window -
controls whether the window is displayed normally, minimized, maximized, or in fullscreen mode.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| value | string | One of: normal, minimized, maximized, fullscreen |

### AutoAttach

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Configuration for automatically
attaching to new targets that are related to an existing target (iframes, workers, service workers).

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| autoAttach | boolean | Whether to auto-attach to related targets |
| waitForDebuggerOnStart | boolean | Whether to pause new targets when attaching |
| flatten | boolean | Whether to use flat session access mode |
| filter | TargetFilter | Filter criteria for which targets to attach |

**Relationships:**

- applies-to Target (Auto-attach configuration applies to target monitoring)

### Breakpoint

[PARTIAL SPEC — file too large to fully analyze (44 parts)] A debugging breakpoint that pauses
execution when reached.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| breakpointId | BreakpointId | Unique identifier for the breakpoint |
| location | Location | Actual breakpoint location in source code |
| resolvedBreakpoints | ResolvedBreakpoint[] | List of resolved breakpoints in the script |

### Script

[PARTIAL SPEC — file too large to fully analyze (44 parts)] A JavaScript or WebAssembly script
parsed by the virtual machine.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| scriptId | Runtime.ScriptId | Unique identifier of the script |
| url | string | URL or name of the script |
| startLine | number | Line offset within the resource |
| startColumn | number | Column offset within the resource |
| endLine | number | Last line of the script |
| endColumn | number | Length of the last line |
| executionContextId | Runtime.ExecutionContextId | Script creation context |
| hash | string | Content hash SHA-256 |
| buildId | string | Wasm build_id or JS debugId magic comment |
| sourceMapURL | string | URL of associated source map |
| hasSourceURL | boolean | Whether script has sourceURL |
| isModule | boolean | Whether script is ES6 module |
| length | number | Script length |
| scriptLanguage | Debugger.ScriptLanguage | Language of the script |
| embedderName | string | Name supplied by embedder |
| isLiveEdit | boolean | Generated by live edit operation |
| codeOffset | number | Wasm code section offset |
| debugSymbols | Debugger.DebugSymbols[] | Wasm debug symbols |

### PauseEvent

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Event fired when the virtual machine
stops on breakpoint, exception, or other stop criteria.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| callFrames | CallFrame[] | Call stack when paused |
| reason | string | Pause reason: ambiguous|assert|CSPViolation|debugCommand|DOM|EventListener|exception|instrumentation|OOM|other|promiseRejection|XHR|step |
| data | object | Break-specific auxiliary properties |
| hitBreakpoints | string[] | IDs of hit breakpoints |
| asyncStackTrace | Runtime.StackTrace | Async stack trace if any |
| asyncStackTraceId | Runtime.StackTraceId | Async stack trace ID |

**Relationships:**

- contains CallFrame (Pause event contains call frames representing the call stack)

### Location

[PARTIAL SPEC — file too large to fully analyze (44 parts)] A position in source code identified by
script ID, line, and column.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| scriptId | Runtime.ScriptId | Script identifier |
| lineNumber | number | Line number in the script (0-based) |
| columnNumber | number | Column number in the script (0-based) |

### BreakLocation

[PARTIAL SPEC — file too large to fully analyze (44 parts)] A possible location where a breakpoint
can be set.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| scriptId | Runtime.ScriptId | Script identifier |
| lineNumber | number | Line number |
| columnNumber | number | Column number |
| type | string | Breakpoint type: debuggerStatement|call|return|step |

### DebuggerSession

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents an active debugging session
after debugger is enabled.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| debuggerId | Runtime.UniqueDebuggerId | Unique identifier of the debugger session |
| maxScriptsCacheSize | number | Maximum size of collected scripts cache |

### CallFrame

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents a frame in the JavaScript
call stack during debugging, containing information about the function execution context.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| callFrameId | CallFrameId | Unique identifier for this call frame |
| functionName | string | Name of the function executing in this frame |
| location | Location | Source code location in this frame |
| url | string | URL of the script containing this call frame |

**Relationships:**

- references Script (Call frame references the script being executed)

### SearchMatch

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents a search result found within
JavaScript script content.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| lineNumber | number | Line number where the match was found |
| lineContent | string | Content of the matching line |

**Relationships:**

- belongs to Script (Match is found within a specific script)

### BlackboxConfig

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Configuration that tells the debugger to
skip certain scripts or execution contexts during stepping and pausing.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| patterns | string[] | Array of regex patterns for URLs to blackbox |
| uniqueIds | string[] | Array of execution context IDs to blackbox |
| skipAnonymous | boolean | If true, also ignore scripts with no source URL |

### DebuggerState

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents the current state of the
JavaScript debugger including breakpoints activation and exception handling mode.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| active | boolean | Whether breakpoints are currently active |
| pauseOnExceptionsState | "none"|"caught"|"uncaught"|"all" | Determines when debugger pauses on exceptions |
| maxAsyncCallStackDepth | number | Maximum depth for tracking async call stacks |
| terminateOnResume | boolean | Whether to terminate execution when resumed |

### InstrumentationBreakpoint

[PARTIAL SPEC — file too large to fully analyze (44 parts)] A special breakpoint that fires before
script execution events, useful for intercepting script loading.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| breakpointId | string | Unique identifier for the instrumentation breakpoint |
| instrumentation | "beforeScriptExecution"|"beforeScriptWithSourceMapExecution" | The instrumentation event to break on |

**Relationships:**

- triggers-on Script (Fires before script execution begins)

### FunctionBreakpoint

[PARTIAL SPEC — file too large to fully analyze (44 parts)] A breakpoint that triggers before each
call to a specific function.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| breakpointId | string | Unique identifier for the function breakpoint |
| objectId | Runtime.RemoteObjectId | Object ID of the function to break on |
| condition | string | Optional condition for the breakpoint |

**Relationships:**

- triggers-before CallFrame (Breaks before function is called)

### DebuggerScript

[PARTIAL SPEC — file too large to fully analyze (44 parts)] A JavaScript source file that can be
edited live during debugging

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| scriptId | string | Unique identifier for the script |
| scriptSource | string | The JavaScript source code content |

### HeapSnapshot

[PARTIAL SPEC — file too large to fully analyze (44 parts)] A point-in-time capture of the
JavaScript heap memory

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| heapSnapshotObjectId | string | Unique identifier for heap objects |
| chunk | string | Part of the heap snapshot data |

### SamplingHeapProfile

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Memory profiling data collected via
sampling to identify memory usage patterns

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| head | SamplingHeapProfileNode | Root node of the profile tree |
| samples | SamplingHeapProfileSample[] | Time-ordered memory samples |

**Relationships:**

- contains SamplingHeapProfileNode (Profile contains nodes representing call sites and allocations)

### SamplingHeapProfileNode

[PARTIAL SPEC — file too large to fully analyze (44 parts)] A node in the heap profile tree
representing a function call site with its allocation statistics

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| callFrame | Runtime.CallFrame | Function location in source code |
| selfSize | number | Allocations size in bytes for this node excluding children |
| id | number | Unique node id across all profiles |
| children | SamplingHeapProfileNode[] | Child nodes in the profile tree |

**Relationships:**

- belongs to SamplingHeapProfile (Nodes form the tree structure of a heap profile)

### HeapProfileSample

[PARTIAL SPEC — file too large to fully analyze (44 parts)] A single sample from a sampling heap
profile representing memory allocated at a point in time

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| size | number | Allocation size in bytes attributed to this sample |
| nodeId | number | Id of the corresponding profile tree node |
| ordinal | number | Time-ordered sample ordinal number |

**Relationships:**

- references SamplingHeapProfileNode (Sample references a node in the profile tree)

### Profile

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents a CPU profiling session
containing execution statistics and call tree nodes.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| nodes | ProfileNode[] | The list of profile nodes. First item is the root node. |
| startTime | number | Profiling start timestamp in microseconds. |
| endTime | number | Profiling end timestamp in microseconds. |
| samples | number[] | Ids of samples top nodes. |
| timeDeltas | number[] | Time intervals between adjacent samples in microseconds. |

**Relationships:**

- contains ProfileNode (Profile contains multiple profile nodes forming a call tree)

### ProfileNode

[PARTIAL SPEC — file too large to fully analyze (44 parts)] A node in a CPU profile representing a
function callsite with execution statistics.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| id | number | Unique id of the node. |
| callFrame | Runtime.CallFrame | Function location. |
| hitCount | number | Number of samples where this node was on top of the call stack. |
| children | number[] | Child node ids. |
| deoptReason | string | The reason of being not optimized. |
| positionTicks | PositionTickInfo[] | An array of source position ticks. |

**Relationships:**

- belongs to Profile (Node belongs to a parent profile)
- has-many ProfileNode (Node can have child nodes)

### ScriptCoverage

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Code coverage data for a JavaScript
script, containing function-level coverage information.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| scriptId | Runtime.ScriptId | JavaScript script id. |
| url | string | JavaScript script name or url. |
| functions | FunctionCoverage[] | Functions contained in the script that has coverage data. |

**Relationships:**

- contains FunctionCoverage (Script contains multiple function coverage entries)

### FunctionCoverage

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Coverage data for a JavaScript function,
including source range execution counts.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| functionName | string | JavaScript function name. |
| ranges | CoverageRange[] | Source ranges inside the function with coverage data. |
| isBlockCoverage | boolean | Whether coverage data for this function has block granularity. |

**Relationships:**

- contains CoverageRange (Function contains multiple coverage ranges)

### CoverageRange

[PARTIAL SPEC — file too large to fully analyze (44 parts)] A source range within a function with
associated execution count for code coverage analysis.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| startOffset | number | JavaScript script source offset for the range start. |
| endOffset | number | JavaScript script source offset for the range end. |
| count | number | Collected execution count of the source range. |

### RemoteObject

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Mirror object that represents a
JavaScript value in the browser runtime, used for remote evaluation results.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| type | string | Object type (object, function, undefined, string, number, boolean, symbol, bigint). |
| subtype | string | Object subtype hint for object type values. |
| className | string | Object class (constructor) name for object type values. |
| value | any | Remote object value for primitive values or JSON values. |
| unserializableValue | string | Primitive value which cannot be JSON-stringified. |
| description | string | String representation of the object. |
| deepSerializedValue | DeepSerializedValue | Deep serialized value. |
| objectId | Runtime.RemoteObjectId | Unique object identifier for non-primitive values. |
| preview | ObjectPreview | Preview containing abbreviated property values. |

**Relationships:**

- has-one ObjectPreview (Object can have a preview for abbreviated representation)
- may-contain ExceptionDetails (Object may contain exception details if evaluation threw)

### ExceptionDetails

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Detailed information about an exception
or error that was thrown during script compilation or execution.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| exceptionId | number | Exception id. |
| text | string | Exception text. |
| lineNumber | number | Line number of the exception location (0-based). |
| columnNumber | number | Column number of the exception location (0-based). |
| scriptId | Runtime.ScriptId | Script ID of the exception location. |
| url | string | URL of the exception location. |
| stackTrace | StackTrace | JavaScript stack trace if available. |
| exception | RemoteObject | Exception object if available. |
| executionContextId | Runtime.ExecutionContextId | Identifier of the context where exception happened. |
| exceptionMetaData | object | Dictionary with entries of meta data associated with this exception. |

**Relationships:**

- has-one StackTrace (Exception may have an associated stack trace)
- has-one RemoteObject (Exception contains the thrown object)

### StackTrace

[PARTIAL SPEC — file too large to fully analyze (44 parts)] A call stack trace for runtime errors
and assertions, including asynchronous traces.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| description | string | String label of this stack trace for async traces. |
| callFrames | CallFrame[] | JavaScript function call frames. |
| parent | StackTrace | Asynchronous JavaScript stack trace that preceded this stack. |
| parentId | StackTraceId | Asynchronous JavaScript stack trace ID. |

**Relationships:**

- contains CallFrame (Stack trace contains multiple call frames)
- may-link-to StackTrace (May link to parent stack trace for async calls)

### ExecutionContextDescription

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Represents an isolated JavaScript
execution context (e.g., iframe, worker, or main page).

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| id | Runtime.ExecutionContextId | Unique id of the execution context. |
| origin | string | Execution context origin. |
| name | string | Human readable name describing given context. |
| uniqueId | string | System-unique execution context identifier across processes. |
| auxData | object | Embedder-specific auxiliary data. |

### PropertyDescriptor

[PARTIAL SPEC — file too large to fully analyze (44 parts)] Descriptor for an object property,
including its value, accessibility, and metadata.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| name | string | Property name or symbol description. |
| value | RemoteObject | The value associated with the property. |
| writable | boolean | True if the value may be changed (data descriptors only). |
| get | RemoteObject | Function serving as getter for the property. |
| set | RemoteObject | Function serving as setter for the property. |
| configurable | boolean | True if property descriptor type may be changed and property may be deleted. |
| enumerable | boolean | True if property shows up during enumeration. |
| wasThrown | boolean | True if result was thrown during evaluation. |
| isOwn | boolean | True if property is owned for the object. |
| symbol | RemoteObject | Property symbol object if property is of symbol type. |

**Relationships:**

- has-one RemoteObject (Property value is a remote object)

## Requirements

### Requirement: WindowBoundsValidation

The system SHALL validate WindowBounds according to these rules:
- Window state 'minimized', 'maximized', 'fullscreen' cannot be combined with left, top, width, or height

#### Scenario: GetWindowBounds
- **GIVEN** A browser window exists
- **WHEN** getWindowBounds(windowId) is called
- **THEN** Returns current bounds including restored position if minimized

#### Scenario: SetWindowBounds
- **GIVEN** A browser window exists
- **WHEN** setWindowBounds(windowId, bounds) is called
- **THEN** Window position and/or size is updated according to bounds

### Requirement: PermissionValidation

The system SHALL validate Permission according to these rules:
- userVisibleOnly must be true for push permission
- Valid permission names from Chromium permissions API

#### Scenario: SetPermission
- **GIVEN** A browser context exists
- **WHEN** setPermission(permission, setting, origin) is called
- **THEN** Permission is granted/denied/prompted for the specified origin

#### Scenario: GrantPermissions
- **GIVEN** A browser context exists
- **WHEN** grantPermissions(permissions, origin) is called
- **THEN** Specific permissions are granted to origin

### Requirement: HistogramValidation

The system SHALL validate Histogram according to these rules:
- count must equal sum of all bucket counts

#### Scenario: GetAllHistograms
- **GIVEN** Browser is running
- **WHEN** getHistograms(query) is called
- **THEN** Returns all histograms matching the query string

#### Scenario: GetSpecificHistogram
- **GIVEN** Browser is running
- **WHEN** getHistogram(name) is called
- **THEN** Returns histogram with the specified name

### Requirement: BucketValidation

The system SHALL validate Bucket according to these rules:
- low must be less than high
- count must be non-negative

#### Scenario: ValidBucketAccepted
- **GIVEN** A valid Bucket object with all required fields
- **WHEN** The object is validated
- **THEN** Validation passes with no errors

### Requirement: DownloadValidation

The system SHALL validate Download according to these rules:
- state can only be 'inProgress', 'completed', or 'canceled'
- filePath only available when state is 'completed'

#### Scenario: DownloadBegins
- **GIVEN** Page initiates a download
- **WHEN** User clicks download link
- **THEN** downloadWillBegin event fired with guid, url, suggestedFilename

#### Scenario: DownloadProgress
- **GIVEN** Download is in progress
- **WHEN** More data is received
- **THEN** downloadProgress event fired with updated receivedBytes

#### Scenario: DownloadCompletes
- **GIVEN** Download finishes
- **WHEN** All bytes received
- **THEN** downloadProgress event with state='completed' and filePath

#### Scenario: CancelDownload
- **GIVEN** Download in progress
- **WHEN** cancelDownload(guid) is called
- **THEN** Download is stopped and state set to 'canceled'

### Requirement: DOMNodeValidation

The system SHALL validate DOMNode according to these rules:
- nodeId is required for all DOM operations
- Backend will only push node with given id once
- Client must track nodes known to the client

#### Scenario: QueryDomNode
- **GIVEN** Page is loaded
- **WHEN** DOM node is requested by nodeId
- **THEN** Node mirror object is returned with unique id

#### Scenario: GetNodeChildren
- **GIVEN** DOM node exists
- **WHEN** Children are requested
- **THEN** Child nodes array is returned with parentId references

### Requirement: RGBAValidation

The system SHALL validate RGBA according to these rules:
- r, g, b must be in range 0-255
- a must be in range 0-1 if specified

#### Scenario: ValidRGBAAccepted
- **GIVEN** A valid RGBA object with all required fields
- **WHEN** The object is validated
- **THEN** Validation passes with no errors

### Requirement: DOMSearchSessionValidation

The system SHALL validate DOMSearchSession according to these rules:
- searchId must be unique per session
- resultCount must be non-negative
- Session must be discarded when no longer needed to free resources

#### Scenario: SearchDomTreeForText
- **GIVEN** A document is loaded
- **WHEN** performSearch is called with a query string
- **THEN** Returns searchId and resultCount for the matching nodes

#### Scenario: RetrieveSearchResultsInBatch
- **GIVEN** A search session exists with results
- **WHEN** getSearchResults is called with fromIndex and toIndex
- **THEN** Returns array of nodeIds for the requested range

#### Scenario: DiscardSearchSession
- **GIVEN** A search session is no longer needed
- **WHEN** discardSearchResults is called with searchId
- **THEN** Search session is invalidated and resources freed

### Requirement: DOMEventValidation

The system SHALL validate DOMEvent according to these rules:
- Event types: attributeModified, attributeRemoved, characterDataModified, childNodeInserted, childNodeRemoved, childNodeCountUpdated, setChildNodes, shadowRootPushed, shadowRootPopped, pseudoElementAdded, pseudoElementRemoved

#### Scenario: AttributeModifiedEvent
- **GIVEN** An element's attribute is changed via JavaScript or user interaction
- **WHEN** Attribute modification occurs
- **THEN** attributeModifiedPayload event fires with nodeId, name, and new value

#### Scenario: NodeInsertedEvent
- **GIVEN** A new node is added to the DOM
- **WHEN** DOM insertion occurs
- **THEN** childNodeInsertedPayload fires with parentNodeId, previousNodeId, and the new node data

#### Scenario: NodeRemovedEvent
- **GIVEN** A node is removed from the DOM
- **WHEN** DOM removal occurs
- **THEN** childNodeRemovedPayload fires with parentNodeId and removed nodeId

#### Scenario: ShadowRootPushedEvent
- **GIVEN** A shadow root is attached to a host element
- **WHEN** attachShadow is called or shadow DOM is created
- **THEN** shadowRootPushedPayload fires with hostId and the new shadow root node

### Requirement: ComputedStylePropertyValidation

The system SHALL validate ComputedStyleProperty according to these rules:
- name must be a valid CSS property name
- value is the computed/resolved CSS value

#### Scenario: ValidComputedStylePropertyAccepted
- **GIVEN** A valid ComputedStyleProperty object with all required fields
- **WHEN** The object is validated
- **THEN** Validation passes with no errors

### Requirement: DOMStorageValidation

The system SHALL validate DOMStorage according to these rules:
- Storage ID must specify isLocalStorage boolean

#### Scenario: ReadStorageItems
- **GIVEN** Page with DOM storage populated
- **WHEN** getDOMStorageItems() is called
- **THEN** Returns all storage key-value pairs

#### Scenario: AddStorageItem
- **GIVEN** DOM storage initialized
- **WHEN** setDOMStorageItem() is called
- **THEN** New key-value pair added to storage

#### Scenario: RemoveStorageItem
- **GIVEN** Item exists in storage
- **WHEN** removeDOMStorageItem() is called
- **THEN** Item removed from storage

#### Scenario: ClearStorage
- **GIVEN** Storage has items
- **WHEN** clear() is called
- **THEN** All items removed from storage

### Requirement: StorageIdValidation

The system SHALL validate StorageId according to these rules:
- isLocalStorage is required boolean

#### Scenario: ValidStorageIdAccepted
- **GIVEN** A valid StorageId object with all required fields
- **WHEN** The object is validated
- **THEN** Validation passes with no errors

### Requirement: DeviceOrientationValidation

The system SHALL validate DeviceOrientation according to these rules:
- Alpha: 0-360, Beta: -180 to 180, Gamma: -90 to 90

#### Scenario: SetDeviceOrientation
- **GIVEN** Page loaded
- **WHEN** setDeviceOrientationOverride() is called
- **THEN** Device orientation events report specified angles

#### Scenario: ClearOrientationOverride
- **GIVEN** Orientation is overridden
- **WHEN** clearDeviceOrientationOverride() is called
- **THEN** Device returns to real sensor values

### Requirement: EmulatedEnvironmentValidation

The system SHALL validate EmulatedEnvironment according to these rules:
- ScreenOrientation type must be portraitPrimary|portraitSecondary|landscapePrimary|landscapeSecondary
- DevicePosture type must be continuous|folded

#### Scenario: EmulateScreenOrientation
- **GIVEN** Page loaded
- **WHEN** setScreenOrientationOverride() is called
- **THEN** screen.orientation reports specified orientation

#### Scenario: EmulateFoldedDevice
- **GIVEN** Page supports foldable emulation
- **WHEN** setDevicePostureOverride() sets folded
- **THEN** Screen segments and viewport adapt to folded state

### Requirement: ScreenOrientationValidation

The system SHALL validate ScreenOrientation according to these rules:
- Type must be portraitPrimary|portraitSecondary|landscapePrimary|landscapeSecondary

#### Scenario: ValidScreenOrientationAccepted
- **GIVEN** A valid ScreenOrientation object with all required fields
- **WHEN** The object is validated
- **THEN** Validation passes with no errors

### Requirement: SensorReadingValidation

The system SHALL validate SensorReading according to these rules:
- Must contain exactly one of single, xyz, or quaternion

#### Scenario: ValidSensorReadingAccepted
- **GIVEN** A valid SensorReading object with all required fields
- **WHEN** The object is validated
- **THEN** Validation passes with no errors

### Requirement: UserAgentMetadataValidation

The system SHALL validate UserAgentMetadata according to these rules:
- platform, platformVersion, architecture, model, and mobile are required

#### Scenario: EmulateMobileBrowser
- **GIVEN** Page loaded
- **WHEN** setUserAgentOverride() with mobile: true
- **THEN** Navigator reports mobile device and platform

### Requirement: RequestPatternValidation

The system SHALL validate RequestPattern according to these rules:
- At least one of urlPattern, resourceType, or requestMethod must be specified

#### Scenario: PatternMatching
- **GIVEN** Enable called with patterns array
- **WHEN** Request is made
- **THEN** Only requests matching any pattern produce requestPaused events

### Requirement: FulfilledResponseValidation

The system SHALL validate FulfilledResponse according to these rules:
- responseCode must be a valid HTTP status code

#### Scenario: MockApiResponse
- **GIVEN** Request paused for API call
- **WHEN** fulfillRequest called with custom body and 200
- **THEN** Page receives mocked response with custom body

#### Scenario: FailRequestWithError
- **GIVEN** Request paused
- **WHEN** failRequest called with errorReason
- **THEN** Request fails with specified network error

### Requirement: RequestContinuationValidation

The system SHALL validate RequestContinuation according to these rules:
- Headers override does not extend to subsequent redirect hops

#### Scenario: ModifyRequestUrl
- **GIVEN** Request paused
- **WHEN** continueRequest called with different url
- **THEN** Request proceeds to modified URL transparently

#### Scenario: AddCustomHeaders
- **GIVEN** Request paused
- **WHEN** continueRequest called with headers
- **THEN** Request includes additional headers

### Requirement: ResponseBodyValidation

The system SHALL validate ResponseBody according to these rules:
- Only available for requests paused at response stage

#### Scenario: ReadResponseBody
- **GIVEN** Request paused at response stage
- **WHEN** getResponseBody called
- **THEN** Returns response body and encoding flag

### Requirement: ScreenshotParamsValidation

The system SHALL validate ScreenshotParams according to these rules:
- Quality must be between 0 and 100 if specified

#### Scenario: ValidScreenshotParamsAccepted
- **GIVEN** A valid ScreenshotParams object with all required fields
- **WHEN** The object is validated
- **THEN** Validation passes with no errors

### Requirement: KeyValidation

The system SHALL validate Key according to these rules:
- Exactly one value field must be present based on type

#### Scenario: ValidKeyAccepted
- **GIVEN** A valid Key object with all required fields
- **WHEN** The object is validated
- **THEN** Validation passes with no errors

### Requirement: IndexedDBDatabaseValidation

The system SHALL validate IndexedDBDatabase according to these rules:
- At least one of securityOrigin, storageKey, or storageBucket must be specified

#### Scenario: RequestDatabase
- **GIVEN** User needs to access IndexedDB data
- **WHEN** requestDatabase() is called with valid database name
- **THEN** Returns database with array of object stores

#### Scenario: DeleteDatabase
- **GIVEN** Database exists in storage
- **WHEN** deleteDatabase() is called
- **THEN** Database is removed from storage

### Requirement: TouchPointValidation

The system SHALL validate TouchPoint according to these rules:
- Coordinates must be non-negative numbers
- Rotation and tilt angles must be within valid ranges

#### Scenario: PerformTouchGesture
- **GIVEN** Touch device available
- **WHEN** TouchPoint is created with coordinates
- **THEN** Touch event dispatched at specified viewport location

### Requirement: DragDataValidation

The system SHALL validate DragData according to these rules:
- At least one drag data item required
- dragOperationsMask must be valid bit field

#### Scenario: InitiateDragOperation
- **GIVEN** User initiates drag
- **WHEN** DragData is created with items
- **THEN** Drag operation can be performed with specified data and allowed operations

### Requirement: DragDataItemValidation

The system SHALL validate DragDataItem according to these rules:
- mimeType and data are required
- title only valid for text/uri-list
- baseURL only valid for text/html

#### Scenario: ValidDragDataItemAccepted
- **GIVEN** A valid DragDataItem object with all required fields
- **WHEN** The object is validated
- **THEN** Validation passes with no errors

### Requirement: NetworkRequestValidation

The system SHALL validate NetworkRequest according to these rules:
- URL must be valid HTTP(s) URL
- Method must be valid HTTP verb

#### Scenario: CaptureOutgoingRequest
- **GIVEN** Page navigates to URL
- **WHEN** Network request is made
- **THEN** Request object captured with all headers and timing

### Requirement: NetworkResponseValidation

The system SHALL validate NetworkResponse according to these rules:
- Status code must be valid HTTP status
- Headers must be well-formed

#### Scenario: CaptureResponse
- **GIVEN** Server responds to request
- **WHEN** Response received
- **THEN** Response object captured with status, headers, and timing

### Requirement: CookieValidation

The system SHALL validate Cookie according to these rules:
- Domain must be valid domain string
- Expires must be -1 or future timestamp
- SourcePort must be -1 or 1-65535

#### Scenario: ReadCookie
- **GIVEN** Page has cookies
- **WHEN** Cookie retrieved
- **THEN** Cookie object returned with all attributes

### Requirement: ResourceTimingValidation

The system SHALL validate ResourceTiming according to these rules:
- All timing values must be non-negative ms offsets from requestTime

#### Scenario: MeasureRequestLatency
- **GIVEN** Network request initiated
- **WHEN** Request completes
- **THEN** ResourceTiming captures all latency phases

### Requirement: SecurityDetailsValidation

The system SHALL validate SecurityDetails according to these rules:
- Protocol must be valid TLS/QUIC protocol
- Certificate dates must be valid timestamps

#### Scenario: InspectSecurityDetails
- **GIVEN** HTTPS request made
- **WHEN** Response received
- **THEN** SecurityDetails contains TLS and certificate info

### Requirement: CachedResourceValidation

The system SHALL validate CachedResource according to these rules:
- URL must be valid
- BodySize must be non-negative

#### Scenario: AccessCachedResource
- **GIVEN** Resource previously fetched
- **WHEN** Resource requested from cache
- **THEN** CachedResource returned with stored response

### Requirement: InitiatorValidation

The system SHALL validate Initiator according to these rules:
- Type must be valid initiator type

#### Scenario: TraceRequestOrigin
- **GIVEN** Network request triggered
- **WHEN** Request captured
- **THEN** Initiator shows what caused the request

### Requirement: WebSocketFrameValidation

The system SHALL validate WebSocketFrame according to these rules:
- Opcode must be valid WebSocket opcode
- Payload must be valid UTF-8 or base64

#### Scenario: CaptureWebsocketMessage
- **GIVEN** WebSocket connection open
- **WHEN** Message received
- **THEN** WebSocketFrame contains message data

### Requirement: InspectModeValidation

The system SHALL validate InspectMode according to these rules:
- Mode must be one of: searchForNode, searchForUAShadowDOM, captureAreaScreenshot, none

#### Scenario: ValidInspectModeAccepted
- **GIVEN** A valid InspectMode object with all required fields
- **WHEN** The object is validated
- **THEN** Validation passes with no errors

### Requirement: FrameValidation

The system SHALL validate Frame according to these rules:
- Frame ID must be unique within a page
- URL must be valid HTTP/HTTPS for remote frames

#### Scenario: NavigateFrame
- **GIVEN** A page with an iframe
- **WHEN** Frame navigates to a new URL
- **THEN** Frame object updated with new URL, loaderId, and security context

### Requirement: WebAppManifestValidation

The system SHALL validate WebAppManifest according to these rules:
- Name is required for installation
- Start URL must be within scope
- Icons must meet size requirements

#### Scenario: ParseManifest
- **GIVEN** A valid web app manifest JSON
- **WHEN** Browser parses the manifest
- **THEN** WebAppManifest object created with all properties

#### Scenario: InstallPwa
- **GIVEN** Valid WebAppManifest with icons
- **WHEN** User installs the PWA
- **THEN** App installed with configured file handlers and shortcuts

### Requirement: NavigationEntryValidation

The system SHALL validate NavigationEntry according to these rules:
- ID must be unique within session history
- URL must be valid

#### Scenario: NavigateBack
- **GIVEN** Browser history with multiple entries
- **WHEN** User navigates back
- **THEN** NavigationEntry returned with previous URL and transition type

### Requirement: ViewportValidation

The system SHALL validate Viewport according to these rules:
- Width and height must be positive
- Scale must be positive

#### Scenario: CaptureScreenshot
- **GIVEN** A page loaded in browser
- **WHEN** Screenshot captured with viewport config
- **THEN** Image captured for the specified viewport area

### Requirement: FrameResourceValidation

The system SHALL validate FrameResource according to these rules:
- URL must be valid

#### Scenario: ListFrameResources
- **GIVEN** A frame with loaded resources
- **WHEN** Frame resources are enumerated
- **THEN** List of FrameResource objects returned with type, mimeType, and loading status

### Requirement: PWAValidation

The system SHALL validate PWA according to these rules:
- manifestId corresponds to isolated-app:// for IWAs or URL for standard PWAs

#### Scenario: GetOsAppState
- **GIVEN** A PWA installed on the system
- **WHEN** Query OS app state by manifestId
- **THEN** Returns badgeCount and registered fileHandlers

#### Scenario: InstallPwa
- **GIVEN** Valid manifestId and optional installUrlOrBundleUrl
- **WHEN** Install command called
- **THEN** PWA installed and ready to launch

#### Scenario: UninstallPwa
- **GIVEN** An installed PWA
- **WHEN** Uninstall called with manifestId
- **THEN** App uninstalled and any open windows closed

#### Scenario: LaunchPwa
- **GIVEN** An installed PWA
- **WHEN** Launch called with manifestId and optional URL
- **THEN** App opens, returns TargetID for attachment

#### Scenario: LaunchFilesInApp
- **GIVEN** PWA with file handlers and files on system
- **WHEN** launchFilesInApp called
- **THEN** Files opened in app, returns TargetIDs for each page

### Requirement: FileHandlerValidation

The system SHALL validate FileHandler according to these rules:
- Action must be a valid URL within app scope
- At least one accept entry required

#### Scenario: RegisterFileHandler
- **GIVEN** App manifest with file handler
- **WHEN** PWA installed
- **THEN** OS registers handler for specified file types

### Requirement: InstallabilityErrorValidation

The system SHALL validate InstallabilityError according to these rules:
- errorId must be a known installability error code

#### Scenario: CheckInstallability
- **GIVEN** A web page
- **WHEN** Checking if installable as PWA
- **THEN** Returns list of InstallabilityError if not installable

### Requirement: FileChooserEventValidation

The system SHALL validate FileChooserEvent according to these rules:
- Mode must be either selectSingle or selectMultiple

#### Scenario: ValidFileChooserEventAccepted
- **GIVEN** A valid FileChooserEvent object with all required fields
- **WHEN** The object is validated
- **THEN** Validation passes with no errors

### Requirement: PreloadingStatusValidation

The system SHALL validate PreloadingStatus according to these rules:
- Valid values: Pending, Running, Ready, Success, Failure, NotSupported

#### Scenario: ValidPreloadingStatusAccepted
- **GIVEN** A valid PreloadingStatus object with all required fields
- **WHEN** The object is validated
- **THEN** Validation passes with no errors

### Requirement: PrefetchStatusValidation

The system SHALL validate PrefetchStatus according to these rules:
- Multiple valid values covering eligibility, network errors, cookie issues, proxy issues, and privacy decoys

#### Scenario: ValidPrefetchStatusAccepted
- **GIVEN** A valid PrefetchStatus object with all required fields
- **WHEN** The object is validated
- **THEN** Validation passes with no errors

### Requirement: PrerenderFinalStatusValidation

The system SHALL validate PrerenderFinalStatus according to these rules:
- Covers activation, destruction, device limitations, scheme errors, CSP blocks, process issues, network errors, user gestures, DevTools disabling, and memory/battery constraints

#### Scenario: ValidPrerenderFinalStatusAccepted
- **GIVEN** A valid PrerenderFinalStatus object with all required fields
- **WHEN** The object is validated
- **THEN** Validation passes with no errors

### Requirement: SecurityStateValidation

The system SHALL validate SecurityState according to these rules:
- Valid values: unknown, neutral, insecure, secure, info, insecure-broken

#### Scenario: ValidSecurityStateAccepted
- **GIVEN** A valid SecurityState object with all required fields
- **WHEN** The object is validated
- **THEN** Validation passes with no errors

### Requirement: SafetyTipInfoValidation

The system SHALL validate SafetyTipInfo according to these rules:
- Valid values: badReputation, lookalike

#### Scenario: ValidSafetyTipInfoAccepted
- **GIVEN** A valid SafetyTipInfo object with all required fields
- **WHEN** The object is validated
- **THEN** Validation passes with no errors

### Requirement: ServiceWorkerVersionValidation

The system SHALL validate ServiceWorkerVersion according to these rules:
- runningStatus must be one of: stopped, starting, running, stopping
- status must be one of: new, installing, installed, activating, activated, redundant

#### Scenario: ValidServiceWorkerVersionAccepted
- **GIVEN** A valid ServiceWorkerVersion object with all required fields
- **WHEN** The object is validated
- **THEN** Validation passes with no errors

### Requirement: StorageBucketInfoValidation

The system SHALL validate StorageBucketInfo according to these rules:
- durability must be either 'relaxed' or 'strict'
- quota must be non-negative

#### Scenario: ValidStorageBucketInfoAccepted
- **GIVEN** A valid StorageBucketInfo object with all required fields
- **WHEN** The object is validated
- **THEN** Validation passes with no errors

### Requirement: SharedStorageMetadataValidation

The system SHALL validate SharedStorageMetadata according to these rules:
- remainingBudget must be between 0 and 1
- bytesUsed must be non-negative
- length must be non-negative

#### Scenario: ValidSharedStorageMetadataAccepted
- **GIVEN** A valid SharedStorageMetadata object with all required fields
- **WHEN** The object is validated
- **THEN** Validation passes with no errors

### Requirement: AttributionReportingSourceRegistrationValidation

The system SHALL validate AttributionReportingSourceRegistration according to these rules:
- type must be 'navigation' or 'event'
- triggerDataMatching must be 'exact' or 'modulus'
- expiry must be positive

#### Scenario: ValidAttributionReportingSourceRegistrationAccepted
- **GIVEN** A valid AttributionReportingSourceRegistration object with all required fields
- **WHEN** The object is validated
- **THEN** Validation passes with no errors

### Requirement: AttributionReportingTriggerRegistrationValidation

The system SHALL validate AttributionReportingTriggerRegistration according to these rules:
- sourceRegistrationTimeConfig must be 'include' or 'exclude'

#### Scenario: ValidAttributionReportingTriggerRegistrationAccepted
- **GIVEN** A valid AttributionReportingTriggerRegistration object with all required fields
- **WHEN** The object is validated
- **THEN** Validation passes with no errors

### Requirement: RelatedWebsiteSetValidation

The system SHALL validate RelatedWebsiteSet according to these rules:
- primarySites must contain at least one site

#### Scenario: ValidRelatedWebsiteSetAccepted
- **GIVEN** A valid RelatedWebsiteSet object with all required fields
- **WHEN** The object is validated
- **THEN** Validation passes with no errors

### Requirement: TrustTokensValidation

The system SHALL validate TrustTokens according to these rules:
- count must be non-negative

#### Scenario: ValidTrustTokensAccepted
- **GIVEN** A valid TrustTokens object with all required fields
- **WHEN** The object is validated
- **THEN** Validation passes with no errors

### Requirement: UsageForTypeValidation

The system SHALL validate UsageForType according to these rules:
- storageType must be a valid StorageType
- usage must be non-negative

#### Scenario: ValidUsageForTypeAccepted
- **GIVEN** A valid UsageForType object with all required fields
- **WHEN** The object is validated
- **THEN** Validation passes with no errors

### Requirement: CacheStorageValidation

The system SHALL validate CacheStorage according to these rules:
- origin must be a valid security origin

#### Scenario: TrackCacheStorageChanges
- **GIVEN** Cache storage exists for an origin
- **WHEN** trackCacheStorageForOrigin is called
- **THEN** Events are emitted when cache contents or list is modified

### Requirement: IndexedDBValidation

The system SHALL validate IndexedDB according to these rules:
- origin must be a valid security origin

#### Scenario: TrackIndexeddbChanges
- **GIVEN** IndexedDB databases exist for an origin
- **WHEN** trackIndexedDBForOrigin is called
- **THEN** Events emitted when database list or contents change

### Requirement: SharedStorageValidation

The system SHALL validate SharedStorage according to these rules:
- ownerOrigin must be valid security origin
- Access scope must be valid enum value

#### Scenario: TrackSharedStorageAccess
- **GIVEN** Shared storage exists for an origin
- **WHEN** setSharedStorageTracking is enabled
- **THEN** Events emitted for all shared storage access operations

### Requirement: InterestGroupValidation

The system SHALL validate InterestGroup according to these rules:
- ownerOrigin must be valid origin
- name must not be empty

#### Scenario: GetInterestGroupDetails
- **GIVEN** Interest group exists
- **WHEN** getInterestGroupDetails is called
- **THEN** Returns group configuration and metadata

### Requirement: InterestGroupAuctionValidation

The system SHALL validate InterestGroupAuction according to these rules:
- uniqueAuctionId must be unique per auction session

#### Scenario: ValidInterestGroupAuctionAccepted
- **GIVEN** A valid InterestGroupAuction object with all required fields
- **WHEN** The object is validated
- **THEN** Validation passes with no errors

### Requirement: AttributionReportValidation

The system SHALL validate AttributionReport according to these rules:
- url must be valid
- body must be serializable

#### Scenario: SendPendingReports
- **GIVEN** Attribution reports are queued
- **WHEN** sendPendingAttributionReports is called
- **THEN** All pending reports are sent immediately and count returned

### Requirement: TrustTokenValidation

The system SHALL validate TrustToken according to these rules:
- issuerOrigin must be valid origin

#### Scenario: GetTrustTokens
- **GIVEN** Browser context exists
- **WHEN** getTrustTokens is called
- **THEN** Returns count of stored tokens per issuer

### Requirement: StorageQuotaValidation

The system SHALL validate StorageQuota according to these rules:
- usage and quota must be non-negative numbers
- quota can be overridden for testing

#### Scenario: GetStorageQuota
- **GIVEN** Origin has storage
- **WHEN** getUsageAndQuota is called
- **THEN** Returns current usage, quota, and breakdown by type

### Requirement: TargetValidation

The system SHALL validate Target according to these rules:
- Target ID must be unique within the browsing session
- Window state must be one of: normal, minimized, maximized, fullscreen

#### Scenario: CreateNewPageTarget
- **GIVEN** Browser context exists
- **WHEN** createTarget() is called with URL
- **THEN** New target is created and returns targetId

#### Scenario: ActivateTarget
- **GIVEN** Target exists
- **WHEN** activateTarget() is called
- **THEN** Target window is focused and brought to foreground

#### Scenario: CloseTarget
- **GIVEN** Target exists
- **WHEN** closeTarget() is called
- **THEN** Target is closed and returns success=true

#### Scenario: TargetCrashes
- **GIVEN** Target is active
- **WHEN** Browser process crashes
- **THEN** targetCrashedPayload event is issued with status and errorCode

### Requirement: SessionValidation

The system SHALL validate Session according to these rules:
- Session ID must be unique within the connection
- A target can have multiple sessions attached

#### Scenario: AttachToTarget
- **GIVEN** Target exists
- **WHEN** attachToTarget() is called
- **THEN** Returns sessionId for communication with target

#### Scenario: DetachFromTarget
- **GIVEN** Session is attached to target
- **WHEN** detachFromTarget() is called
- **THEN** Session is detached and can no longer communicate with target

#### Scenario: ReceiveMessageFromTarget
- **GIVEN** Session is attached
- **WHEN** Target sends protocol message
- **THEN** receivedMessageFromTargetPayload event is issued

### Requirement: WindowStateValidation

The system SHALL validate WindowState according to these rules:
- Value must be one of: normal, minimized, maximized, fullscreen

#### Scenario: CreateWindowWithSpecificState
- **GIVEN** Browser supports new windows
- **WHEN** createTarget() is called with windowState
- **THEN** New window opens in specified state

### Requirement: AutoAttachValidation

The system SHALL validate AutoAttach according to these rules:
- When autoAttach is true, waitForDebuggerOnStart must be specified

#### Scenario: EnableAutoAttach
- **GIVEN** Session is attached to target
- **WHEN** setAutoAttach() is called with autoAttach=true
- **THEN** Automatically attaches to all existing and future related targets

#### Scenario: DisableAutoAttach
- **GIVEN** Auto-attach is enabled
- **WHEN** setAutoAttach() is called with autoAttach=false
- **THEN** Detaches from all currently attached targets

### Requirement: LocationValidation

The system SHALL validate Location according to these rules:
- Line number must be non-negative

#### Scenario: ValidLocationAccepted
- **GIVEN** A valid Location object with all required fields
- **WHEN** The object is validated
- **THEN** Validation passes with no errors

### Requirement: CallFrameValidation

The system SHALL validate CallFrame according to these rules:
- CallFrameId is required for frame operations like restartFrame

#### Scenario: RestartCallFrame
- **GIVEN** Debugger is paused at a breakpoint
- **WHEN** restartFrame is called with callFrameId
- **THEN** Execution restarts from the beginning of that function

#### Scenario: InspectCallStack
- **GIVEN** Debugger is paused
- **WHEN** Debugger.paused event is received
- **THEN** Call stack contains all CallFrames from current to root

### Requirement: SearchMatchValidation

The system SHALL validate SearchMatch according to these rules:
- searchInContent returns array of SearchMatch objects

#### Scenario: SearchForTextInScript
- **GIVEN** A script loaded in the browser
- **WHEN** searchInContent is called with query string
- **THEN** Returns all lines containing the query with line numbers

### Requirement: BlackboxConfigValidation

The system SHALL validate BlackboxConfig according to these rules:
- Patterns are regex expressions matched against script URLs

#### Scenario: BlackboxByUrlPatterns
- **GIVEN** Scripts with known URLs
- **WHEN** setBlackboxPatterns is called with regex patterns
- **THEN** Debugger skips stepping/pausing in matching scripts

#### Scenario: BlackboxExecutionContexts
- **GIVEN** Known execution contexts
- **WHEN** setBlackboxExecutionContexts is called with uniqueIds
- **THEN** Debugger ignores those contexts completely

### Requirement: DebuggerStateValidation

The system SHALL validate DebuggerState according to these rules:
- pauseOnExceptionsState must be one of: none, caught, uncaught, all
- maxAsyncCallStackDepth of 0 disables async stack tracking

#### Scenario: EnableAllBreakpoints
- **GIVEN** Debugger with breakpoints set
- **WHEN** setBreakpointsActive is called with active=true
- **THEN** All breakpoints become active and can trigger

#### Scenario: PauseOnUncaughtExceptions
- **GIVEN** Debugger is running
- **WHEN** setPauseOnExceptions is called with state='uncaught'
- **THEN** Debugger pauses only on uncaught exceptions

#### Scenario: EnableAsyncStackTraces
- **GIVEN** Debugger is running
- **WHEN** setAsyncCallStackDepth is called with maxDepth > 0
- **THEN** Async call stacks are collected up to that depth

### Requirement: InstrumentationBreakpointValidation

The system SHALL validate InstrumentationBreakpoint according to these rules:
- instrumentation must be a valid instrumentation name

#### Scenario: BreakBeforeScriptExecution
- **GIVEN** Page loading scripts
- **WHEN** setInstrumentationBreakpoint is called with 'beforeScriptExecution'
- **THEN** Debugger pauses before each script runs

### Requirement: FunctionBreakpointValidation

The system SHALL validate FunctionBreakpoint according to these rules:
- objectId must reference a valid function object

#### Scenario: BreakOnFunctionCall
- **GIVEN** A function object in the runtime
- **WHEN** setBreakpointOnFunctionCall is called with objectId
- **THEN** Debugger pauses before each invocation of that function

### Requirement: DebuggerScriptValidation

The system SHALL validate DebuggerScript according to these rules:
- dryRun mode allowed to test changes without applying

#### Scenario: EditScriptSource
- **GIVEN** A script loaded in the debugger
- **WHEN** setScriptSource is called with new source
- **THEN** Script is updated and call stack may be restarted if top frame affected

### Requirement: HeapSnapshotValidation

The system SHALL validate HeapSnapshot according to these rules:
- Can track object allocations and deallocations over time

#### Scenario: ValidHeapSnapshotAccepted
- **GIVEN** A valid HeapSnapshot object with all required fields
- **WHEN** The object is validated
- **THEN** Validation passes with no errors

### Requirement: SamplingHeapProfileValidation

The system SHALL validate SamplingHeapProfile according to these rules:
- Samples are unique across profiles retrieved between startSampling and stopSampling

#### Scenario: CollectMemoryProfile
- **GIVEN** Application is running with heap profiling enabled
- **WHEN** stopSampling is called
- **THEN** Returns sampling profile with allocation statistics by function

### Requirement: SamplingHeapProfileNodeValidation

The system SHALL validate SamplingHeapProfileNode according to these rules:
- Ids are unique across all profiles collected between startSampling and stopSampling

#### Scenario: ValidSamplingHeapProfileNodeAccepted
- **GIVEN** A valid SamplingHeapProfileNode object with all required fields
- **WHEN** The object is validated
- **THEN** Validation passes with no errors

### Requirement: HeapProfileSampleValidation

The system SHALL validate HeapProfileSample according to these rules:
- Ordinal is unique across all profiles retrieved between startSampling and stopSampling

#### Scenario: ValidHeapProfileSampleAccepted
- **GIVEN** A valid HeapProfileSample object with all required fields
- **WHEN** The object is validated
- **THEN** Validation passes with no errors

### Requirement: ProfileValidation

The system SHALL validate Profile according to these rules:
- startTime must be less than endTime

#### Scenario: ValidProfileAccepted
- **GIVEN** A valid Profile object with all required fields
- **WHEN** The object is validated
- **THEN** Validation passes with no errors

### Requirement: CoverageRangeValidation

The system SHALL validate CoverageRange according to these rules:
- startOffset must be less than endOffset
- count must be non-negative

#### Scenario: ValidCoverageRangeAccepted
- **GIVEN** A valid CoverageRange object with all required fields
- **WHEN** The object is validated
- **THEN** Validation passes with no errors

### Requirement: RemoteObjectValidation

The system SHALL validate RemoteObject according to these rules:
- objectId required for non-primitive values

#### Scenario: ValidRemoteObjectAccepted
- **GIVEN** A valid RemoteObject object with all required fields
- **WHEN** The object is validated
- **THEN** Validation passes with no errors

### Requirement: StackTraceValidation

The system SHALL validate StackTrace according to these rules:
- callFrames must contain at least one frame

#### Scenario: ValidStackTraceAccepted
- **GIVEN** A valid StackTrace object with all required fields
- **WHEN** The object is validated
- **THEN** Validation passes with no errors

### Requirement: PropertyDescriptorValidation

The system SHALL validate PropertyDescriptor according to these rules:
- Either value or get/set should be present, not both (data vs accessor descriptor)

#### Scenario: ValidPropertyDescriptorAccepted
- **GIVEN** A valid PropertyDescriptor object with all required fields
- **WHEN** The object is validated
- **THEN** Validation passes with no errors

### Requirement: Constructor

The system SHALL initializes the playwright instance with browser channels and instrumentation listeners to track open resources.

#### Scenario: PlaywrightInitialization
- **GIVEN** New Playwright instance requested
- **WHEN** createPlaywright is called
- **THEN** All browser channels instantiated, instrumentation listeners registered for tracking pages and
browsers

### Requirement: Allbrowsers

The system SHALL returns all currently open browser instances managed by this playwright instance.

#### Scenario: ListAllOpenBrowsers
- **GIVEN** Multiple browsers are open
- **WHEN** allBrowsers() is called
- **THEN** Returns array of all browser instances

### Requirement: Allpages

The system SHALL returns all currently open pages across all browser contexts.

#### Scenario: ListAllOpenPages
- **GIVEN** Multiple pages are open across browsers
- **WHEN** allPages() is called
- **THEN** Returns array of all page instances

### Requirement: Newcontext

The system SHALL creates a new browser context with specified options including client certificates, storage state, and proxy settings.

#### Scenario: CreateContextWithClientCertificates
- **GIVEN** clientCertificates option provided
- **WHEN** newContext is called
- **THEN** ClientCertificatesProxy created and proxy override applied

#### Scenario: CreateContextWithStorageState
- **GIVEN** storageState option provided
- **WHEN** newContext is called
- **THEN** Storage state restored on context

#### Scenario: ContextCreationFails
- **GIVEN** Context creation throws error
- **WHEN** newContext is called
- **THEN** Partial context and proxy cleaned up, error thrown

### Requirement: Newcontextforreuse

The system SHALL creates a new context for reuse or resets an existing reusable context with new parameters.

#### Scenario: ReuseExistingContext
- **GIVEN** Hash matches existing reusable context and context can reset
- **WHEN** newContextForReuse is called
- **THEN** Existing context reset for reuse

#### Scenario: CreateNewReusableContext
- **GIVEN** No matching reusable context or hash mismatch
- **WHEN** newContextForReuse is called
- **THEN** New context created and stored for reuse

### Requirement: Close

The system SHALL closes the browser and all its contexts, waiting for disconnection event.

#### Scenario: CloseBrowserGracefully
- **GIVEN** Browser is connected
- **WHEN** close is called
- **THEN** Browser process closes and waits for Disconnected event

#### Scenario: CloseAlreadyClosingBrowser
- **GIVEN** Browser is already closing
- **WHEN** close is called
- **THEN** Waits for existing close to complete

### Requirement: Killfortests

The system SHALL forcibly kills the browser process for testing purposes.

#### Scenario: KillBrowserProcess
- **GIVEN** Browser process is running
- **WHEN** killForTests is called
- **THEN** Browser process terminated

### Requirement: Downloadcreated

The system SHALL registers a new download initiated by the browser.

#### Scenario: RegisterDownload
- **GIVEN** Download initiated
- **WHEN** _downloadCreated is called
- **THEN** Download object created and tracked in map

### Requirement: Downloadfinished

The system SHALL marks a download as finished, reporting success or error.

#### Scenario: DownloadSucceeds
- **GIVEN** No error provided
- **WHEN** _downloadFinished is called
- **THEN** Download artifact marked as finished successfully

#### Scenario: DownloadFails
- **GIVEN** Error message provided
- **WHEN** _downloadFinished is called
- **THEN** Download artifact marked as finished with error

### Requirement: Videostarted

The system SHALL handles video recording initialization for a context.

#### Scenario: VideoRecordingStarts
- **GIVEN** Video recording enabled
- **WHEN** _videoStarted is called
- **THEN** Artifact created and associated with context and page

### Requirement: Didclose

The system SHALL internal handler for browser close event, notifying all contexts and emitting disconnect event.

#### Scenario: BrowserCloses
- **GIVEN** Browser process terminated
- **WHEN** _didClose is called
- **THEN** All contexts notified, Disconnected event emitted

### Requirement: Sdklanguage

The system SHALL returns the sdk language configured for this browser instance.

#### Scenario: GetSdkLanguage
- **GIVEN** Browser has options
- **WHEN** sdkLanguage is called
- **THEN** Returns SDK language from options or default

### Requirement: Forcontext

The system SHALL creates or retrieves a recorder instance bound to a specific browser context.

#### Scenario: NewContext
- **GIVEN** BrowserContext without existing recorder
- **WHEN** forContext is called
- **THEN** New Recorder instance created and bound to context

#### Scenario: ExistingContext
- **GIVEN** BrowserContext with existing recorder
- **WHEN** forContext is called
- **THEN** Existing Recorder instance returned

### Requirement: Existingforcontext

The system SHALL retrieves existing recorder for context without creating new one.

#### Scenario: NoRecorder
- **GIVEN** Context without recorder
- **WHEN** existingForContext is called
- **THEN** undefined returned

### Requirement: Close

The system SHALL shuts down recorder and releases all resources.

#### Scenario: CleanShutdown
- **GIVEN** Recorder active with bound context
- **WHEN** close is called
- **THEN** All event listeners removed, context bindings cleared

### Requirement: Mode

The system SHALL returns the current recording mode.

### Requirement: Setmode

The system SHALL transitions recorder to specified mode.

#### Scenario: PauseRecording
- **GIVEN** Recorder in recording mode
- **WHEN** setMode('pause') is called
- **THEN** Recording paused, UI updated

#### Scenario: ResumeRecording
- **GIVEN** Recorder in paused mode
- **WHEN** setMode('recording') is called
- **THEN** Recording resumed

### Requirement: Pause

The system SHALL pauses the recording session.

### Requirement: Resume

The system SHALL resumes the recording session.

### Requirement: Paused

The system SHALL checks if recorder is currently paused.

### Requirement: Sethighlightedselector

The system SHALL highlights dom elements matching the specified selector in the page.

#### Scenario: ValidSelector
- **GIVEN** Valid CSS selector for existing element
- **WHEN** setHighlightedSelector called
- **THEN** Element highlighted in page overlay

#### Scenario: InvalidSelector
- **GIVEN** Selector matching no elements
- **WHEN** setHighlightedSelector called
- **THEN** No highlight shown

### Requirement: Hidehighlightedselector

The system SHALL removes all highlights from the page.

### Requirement: Sethighlightedariatemplate

The system SHALL sets aria template to display accessibility information in highlight.

### Requirement: Onbeforecall

The system SHALL intercepts sdk method calls before execution to log them.

#### Scenario: RecordAction
- **GIVEN** Page.click called during recording
- **WHEN** onBeforeCall invoked
- **THEN** Action added to call log

### Requirement: Onaftercall

The system SHALL processes sdk method calls after execution.

### Requirement: Onbeforeinputaction

The system SHALL handles input-specific actions before execution.

### Requirement: Oncalllog

The system SHALL records detailed log messages for a specific call.

### Requirement: Updatecalllog

The system SHALL updates call log entries based on current recording state.

### Requirement: Calllog

The system SHALL retrieves current call log entries.

### Requirement: Step

The system SHALL advances recording to next step and updates source code.

#### Scenario: RecordStep
- **GIVEN** Action performed in recording mode
- **WHEN** step is called
- **THEN** Source code regenerated with new action

### Requirement: Setlanguage

The system SHALL sets the programming language for code generation.

#### Scenario: SwitchLanguage
- **GIVEN** Recording in JavaScript
- **WHEN** setLanguage('python') called
- **THEN** Source code regenerated in Python

### Requirement: Usersources

The system SHALL returns all user-edited source files.

### Requirement: Pausedsourceid

The system SHALL returns id of source being edited when recorder paused.

### Requirement: Setrequestinterception

The system SHALL enables or disables request interception for the page.

#### Scenario: EnableInterception
- **GIVEN** Interception disabled
- **WHEN** setRequestInterception(true) is called
- **THEN** Requests are routed through interception handlers

#### Scenario: DisableInterception
- **GIVEN** Interception enabled
- **WHEN** setRequestInterception(false) is called
- **THEN** Requests proceed normally without interception

### Requirement: Setcredentials

The system SHALL sets authentication credentials for the network manager.

#### Scenario: SetCredentials
- **GIVEN** No credentials set
- **WHEN** setCredentials is called with valid credentials
- **THEN** Credentials are stored for use with authenticated requests

### Requirement: Dispose

The system SHALL releases all resources and cleans up network manager state.

#### Scenario: CleanupOnClose
- **GIVEN** Active network session
- **WHEN** dispose is called
- **THEN** All pending requests are cleaned up and resources released

### Requirement: Onresponsestarted

The system SHALL handles the response started event, processing headers and security details.

#### Scenario: ProcessResponseStart
- **GIVEN** Request initiated
- **WHEN** Response starts arriving
- **THEN** Headers parsed, timing recorded, security details extracted, responseStarted event emitted

#### Scenario: ProcessResponseWithRedirect
- **GIVEN** Redirect response received
- **WHEN** Response started event arrives
- **THEN** Redirect information processed and chained

### Requirement: Onresponsecompleted

The system SHALL handles the response completed event, finalizing request state.

#### Scenario: ProcessSuccessfulResponse
- **GIVEN** Response fully received
- **WHEN** Response completed event arrives
- **THEN** Transfer size updated, request finished event emitted, request removed from tracking

#### Scenario: ProcessFailedResponse
- **GIVEN** Response failed
- **WHEN** Response completed with error
- **THEN** Failure status recorded and requestFailed event emitted

### Requirement: Onfetcherror

The system SHALL handles network errors and fetch failures.

#### Scenario: NetworkError
- **GIVEN** Request in flight
- **WHEN** Network error occurs
- **THEN** Request failed event emitted with error details, request cleaned up


## Sub-components

> `BidiNetworkManager` is an orchestrator. Each sub-component below implements one logical block.

### Sub-component: Get

> Implements: `get`

Retrieves the current network request or response data from the internal state.

#### Requirement: RetrieveStoredNetworkData

The system SHALL return the internally stored request or response object.

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Relativetostart

> Implements: `relativeToStart`

Calculates a timestamp value relative to the start time of the network request.

#### Requirement: CalculateRelativeTimestamp

The system SHALL compute the difference between the current time and the request start time.

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Frombidiheaders

> Implements: `fromBidiHeaders`

Converts BiDi protocol headers into a usable format, pushing entries and converting byte values to strings.

#### Requirement: ConvertBidiHeadersToStringMap

The system SHALL iterate through bidi header entries, push each to the result, and convert bidibytesvaluetostring for values.

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Serveraddrfinished

> Implements: `_serverAddrFinished`

Marks the completion of server address resolution and updates the network request state.

#### Requirement: FinalizeServerAddress

The system SHALL update internal state to indicate server address resolution is complete.

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Securitydetailsfinished

> Implements: `_securityDetailsFinished`

Marks the completion of security details retrieval for secure HTTPS connections.

#### Requirement: FinalizeSecurityDetails

The system SHALL update internal state to indicate security details have been retrieved.

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Setrawresponseheaders

> Implements: `setRawResponseHeaders`

Sets raw response headers on the request after verifying the response is complete using isDone.

#### Requirement: SetHeadersAfterVerifyingCompletion

The system SHALL check isdone before applying raw response headers to the request.

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Setresponseheaderssize

> Implements: `setResponseHeadersSize`

Records the size of response headers for tracking transfer metrics.

#### Requirement: RecordHeadersSize

The system SHALL store the size in bytes of the response headers.

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Requestreceivedresponse

> Implements: `requestReceivedResponse`

Processes a received HTTP response and emits the response event to listeners.

#### Requirement: EmitResponseReceivedEvent

The system SHALL process the response object and emit a 'response' event to registered listeners.

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Existingresponse

> Implements: `_existingResponse`

Retrieves or processes an existing response from cache or previous interaction.

#### Requirement: FetchCachedResponse

The system SHALL return any existing response that was previously stored for this request.

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Settransfersize

> Implements: `setTransferSize`

Records the total transfer size of the response including headers and body.

#### Requirement: StoreTransferSize

The system SHALL set the total bytes transferred for this network request.

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Setencodedbodysize

> Implements: `setEncodedBodySize`

Records the encoded body size of the response (after compression).

#### Requirement: StoreEncodedBodySize

The system SHALL set the size of the response body after encoding (e.g., gzip).

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Status

> Implements: `status`

Returns the HTTP status code of the network response.

#### Requirement: ReturnHttpStatus

The system SHALL return the numeric http status code (e.g., 200, 404, 500).

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Timing

> Implements: `timing`

Returns timing information for various stages of the network request lifecycle.

#### Requirement: ReturnTimingMetrics

The system SHALL return an object containing timing data for request phases like dns, connect, and transfer.

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Requestfinished

> Implements: `_requestFinished`

Marks the network request as completed and performs final cleanup.

#### Requirement: FinalizeRequestCompletion

The system SHALL update internal state to indicate the request has finished and perform cleanup.

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Deleterequest

> Implements: `_deleteRequest`

Removes the network request from internal tracking and releases associated resources.

#### Requirement: RemoveRequestFromTracking

The system SHALL delete the request from internal data structures and clean up resources.

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Sethttpversion

> Implements: `_setHttpVersion`

Stores the HTTP protocol version used for the network request.

#### Requirement: RecordHttpVersion

The system SHALL set the http version string (e.g., 'http/1.1', 'http/2').

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Reportrequestfinished

> Implements: `reportRequestFinished`

Reports the completed network request with its response and emits a finished event.

#### Requirement: EmitRequestFinishedEvent

The system SHALL emit a 'finished' event with the request and response objects to notify listeners.

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Setfailuretext

> Implements: `_setFailureText`

Stores the failure reason text describing why the network request failed.

#### Requirement: StoreFailureReason

The system SHALL set the text description of the failure (e.g., 'net::err_connection_refused').

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Requestfailed

> Implements: `requestFailed`

Handles a failed network request and emits a failure event to notify listeners.

#### Requirement: EmitRequestFailureEvent

The system SHALL emit a 'failed' event with the request and canceled flag to notify listeners of the failure.

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Requirement: Continue

The system SHALL continues the intercepted request with optional overrides.

#### Scenario: ContinueUnmodified
- **GIVEN** Request intercepted
- **WHEN** continue() is called with no overrides
- **THEN** Request proceeds as originally intended

#### Scenario: ContinueWithModifications
- **GIVEN** Request intercepted
- **WHEN** continue({url: 'newUrl'}) is called
- **THEN** Request proceeds to modified URL with new headers

#### Scenario: ContinueWithPostData
- **GIVEN** Request intercepted
- **WHEN** continue({postData: 'data'}) is called
- **THEN** Request proceeds with new POST body

### Requirement: Fulfill

The system SHALL fulfills the intercepted request with a custom response.

#### Scenario: FulfillWithHtml
- **GIVEN** Request intercepted
- **WHEN** fulfill({status: 200, body: '<html>'}) is called
- **THEN** Request resolved with custom HTML response

#### Scenario: FulfillWithJson
- **GIVEN** Request intercepted
- **WHEN** fulfill({status: 200, body: '{"key":"value"}'}) is called
- **THEN** Request resolved with JSON response

#### Scenario: FulfillWithErrorStatus
- **GIVEN** Request intercepted
- **WHEN** fulfill({status: 404, body: 'Not Found'}) is called
- **THEN** Request resolved with 404 response

### Requirement: Abort

The system SHALL aborts the intercepted request with an error code.

#### Scenario: AbortWithFailed
- **GIVEN** Request intercepted
- **WHEN** abort('failed') is called
- **THEN** Request failed with generic failure error

#### Scenario: AbortWithAborted
- **GIVEN** Request in progress
- **WHEN** abort('aborted') is called
- **THEN** Request aborted by user

#### Scenario: AbortWithAccessdenied
- **GIVEN** Request intercepted
- **WHEN** abort('accessdenied') is called
- **THEN** Request blocked with access denied


## Sub-components

> `BidiInterceptedRequest` is an orchestrator. Each sub-component below implements one logical block.

### Sub-component: Get

> Implements: `get`

Retrieves the underlying intercepted request object

#### Requirement: Retrieveinterceptedrequest

The system SHALL return the internal request object that was intercepted

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Relativetostart

> Implements: `relativeToStart`

Calculates position relative to request start point

#### Requirement: Calculaterelativeposition

The system SHALL compute and return a position value relative to the start of the request

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Frombidiheaders

> Implements: `fromBidiHeaders`

Converts bidirectional protocol headers to internal header format

#### Requirement: Convertbidiheaders

The system SHALL convert bidi-format headers to internal header representation using bidibytesvaluetostring

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Requirement: Pushheadertostorage

The system SHALL push each converted header to internal header storage

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Requirement: Convertbidibytestostring

The system SHALL convert bidirectional byte values to string format for headers

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Serveraddrfinished

> Implements: `_serverAddrFinished`

Handles completion of server address resolution for the intercepted request

#### Requirement: Handleserveraddresscomplete

The system SHALL process the completion of server address resolution

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Securitydetailsfinished

> Implements: `_securityDetailsFinished`

Handles completion of security details retrieval for the intercepted request

#### Requirement: Handlesecuritydetailscomplete

The system SHALL process the completion of security details retrieval

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Setrawresponseheaders

> Implements: `setRawResponseHeaders`

Sets raw response headers on the intercepted request

#### Requirement: Assignrawresponseheaders

The system SHALL assign raw response headers to the intercepted request if not already done

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Requirement: Checkheadersnotset

The system SHALL check that headers have not already been set using isdone before setting

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Setresponseheaderssize

> Implements: `setResponseHeadersSize`

Sets the size of the response headers in bytes

#### Requirement: Assignheaderssize

The system SHALL set the response headers size value on the intercepted request

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Requestreceivedresponse

> Implements: `requestReceivedResponse`

Signals that a response has been received for the intercepted request

#### Requirement: Emitresponseevent

The system SHALL emit a response received event to notify listeners of the response

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Existingresponse

> Implements: `_existingResponse`

Manages or retrieves existing response data for the intercepted request

#### Requirement: Handleexistingresponse

The system SHALL process existing response data associated with the request

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Settransfersize

> Implements: `setTransferSize`

Sets the transfer size for the intercepted request response

#### Requirement: Assigntransfersize

The system SHALL set the transfer size value for the response body

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Setencodedbodysize

> Implements: `setEncodedBodySize`

Sets the encoded body size for the intercepted request response

#### Requirement: Assignencodedbodysize

The system SHALL set the encoded body size value for the response

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Status

> Implements: `status`

Retrieves the HTTP status code of the intercepted request response

#### Requirement: Getstatuscode

The system SHALL return the http status code from the response

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Timing

> Implements: `timing`

Retrieves timing information for the intercepted request

#### Requirement: Gettimingdata

The system SHALL return timing data associated with the request lifecycle

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Requestfinished

> Implements: `_requestFinished`

Handles completion of the intercepted request lifecycle

#### Requirement: Handlerequestcompletion

The system SHALL process the completion of the request and update internal state

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Deleterequest

> Implements: `_deleteRequest`

Removes or cleans up the intercepted request from internal state

#### Requirement: Removerequest

The system SHALL delete the intercepted request from internal tracking

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Sethttpversion

> Implements: `_setHttpVersion`

Sets the HTTP protocol version for the intercepted request

#### Requirement: Assignhttpversion

The system SHALL set the http version string on the intercepted request

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Reportrequestfinished

> Implements: `reportRequestFinished`

Signals that the intercepted request has completed with its response

#### Requirement: Emitfinishedevent

The system SHALL emit a request finished event with request and response data

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Setfailuretext

> Implements: `_setFailureText`

Sets the failure reason text for a failed intercepted request

#### Requirement: Assignfailurereason

The system SHALL set the failure text describing why the request failed

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Requestfailed

> Implements: `requestFailed`

Signals that the intercepted request has failed

#### Requirement: Emitfailureevent

The system SHALL emit a request failed event with request and cancellation status

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Requirement: Constructor

The system SHALL initializes response object with session and request id for tracking.

#### Scenario: CreateResponseObject
- **GIVEN** Network request in flight
- **WHEN** BidiResponse is instantiated
- **THEN** Response object created with session reference and request ID


## Sub-components

> `BidiResponse` is an orchestrator. Each sub-component below implements one logical block.

### Sub-component: Get

> Implements: `get`

Retrieves the complete response data object including timing, headers, status, and body information.

#### Requirement: Returnresponsedata

The system SHALL return the stored response data object containing all accumulated response properties

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Relativetostart

> Implements: `relativeToStart`

Calculates a timestamp value relative to the request start time for timing-related operations.

#### Requirement: Calculaterelativetime

The system SHALL calculate and return a time value offset from the request start timestamp

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Frombidiheaders

> Implements: `fromBidiHeaders`

Converts BiDi protocol header format into internal header storage format.

#### Requirement: Convertandstoreheaders

The system SHALL convert each bidi header to string format using bidibytesvaluetostring and push to internal headers array

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Serveraddrfinished

> Implements: `_serverAddrFinished`

Handles completion of server address resolution for the network request.

#### Requirement: Handleserveraddrcompletion

The system SHALL process server address resolution completion and update response state

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Securitydetailsfinished

> Implements: `_securityDetailsFinished`

Handles completion of security details retrieval for secure connections.

#### Requirement: Handlesecuritydetailscompletion

The system SHALL process security details completion and update response security state

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Setrawresponseheaders

> Implements: `setRawResponseHeaders`

Stores raw response headers and checks if response is complete.

#### Requirement: Storeandcheckcompletion

The system SHALL store raw headers and determine if response is done by calling isdone

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Setresponseheaderssize

> Implements: `setResponseHeadersSize`

Records the size of response headers for tracking and reporting purposes.

#### Requirement: Recordheaderssize

The system SHALL store the calculated size of response headers

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Requestreceivedresponse

> Implements: `requestReceivedResponse`

Emits an event indicating that a response has been received for the request.

#### Requirement: Emitresponseevent

The system SHALL emit a response received event with the response data to notify subscribers

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Existingresponse

> Implements: `_existingResponse`

Retrieves or manages existing response data that was previously stored.

#### Requirement: Retrieveexistingresponse

The system SHALL return previously stored response data if available

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Settransfersize

> Implements: `setTransferSize`

Records the total transfer size of the response including headers and body.

#### Requirement: Storetransfersize

The system SHALL store the total size of data transferred for this response

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Setencodedbodysize

> Implements: `setEncodedBodySize`

Records the size of the response body after encoding transformations.

#### Requirement: Storeencodedbodysize

The system SHALL store the encoded body size, which may differ from the original body size

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Status

> Implements: `status`

Returns the HTTP status code of the response.

#### Requirement: Returnstatuscode

The system SHALL return the http status code associated with this response

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Timing

> Implements: `timing`

Returns timing information associated with the request-response cycle.

#### Requirement: Returntimingdata

The system SHALL return timing data including timestamps for various request phases

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Requestfinished

> Implements: `_requestFinished`

Handles completion of the network request and updates final response state.

#### Requirement: Handlerequestcompletion

The system SHALL process request completion and finalize response state

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Deleterequest

> Implements: `_deleteRequest`

Removes the request from internal tracking and releases associated resources.

#### Requirement: Removerequestdata

The system SHALL remove all stored data associated with this request

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Sethttpversion

> Implements: `_setHttpVersion`

Stores the HTTP protocol version used for the response.

#### Requirement: Storehttpversion

The system SHALL store the http version string (e.g., '1.1', '2.0')

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Reportrequestfinished

> Implements: `reportRequestFinished`

Emits an event indicating the request has completed with optional response data.

#### Requirement: Emitfinishedevent

The system SHALL emit a request finished event containing request and response data to notify subscribers

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Setfailuretext

> Implements: `_setFailureText`

Stores the failure error message when a request fails.

#### Requirement: Storefailuremessage

The system SHALL store the error text describing why the request failed

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Sub-component: Requestfailed

> Implements: `requestFailed`

Emits an event indicating the request has failed with cancellation or error details.

#### Requirement: Emitfailureevent

The system SHALL emit a request failed event with request and canceled status to notify subscribers

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

#### Scenario: Unnamed
- **GIVEN** the system is in a valid state
- **WHEN** the operation is invoked
- **THEN** the expected outcome occurs

### Requirement: Addsession

The system SHALL registers a new network session and sets up all event listeners for tracking requests, responses, websockets, and frame attachments.

#### Scenario: MainFrameSessionRegistration
- **GIVEN** New page with main frame
- **WHEN** addSession is called with isMain=true
- **THEN** All network event listeners attached to session, frame tracking enabled

#### Scenario: WorkerSessionRegistration
- **GIVEN** Web worker or service worker
- **WHEN** addSession is called with workerFrame reference
- **THEN** Worker-specific network events wired to frame

### Requirement: Removesession

The system SHALL unregisters a network session and cleans up associated state.

#### Scenario: SessionCleanupOnNavigation
- **GIVEN** Session exists with pending requests
- **WHEN** removeSession is called
- **THEN** Session removed from tracking, no memory leaks

### Requirement: Authenticate

The system SHALL sets authentication credentials for all subsequent requests requiring basic/bearer/digest auth.

#### Scenario: BasicAuthentication
- **GIVEN** Page navigates to protected resource
- **WHEN** authenticate called with credentials
- **THEN** Authorization header sent with subsequent requests

### Requirement: Setoffline

The system SHALL simulates network offline/online conditions for the browser context.

#### Scenario: EnableOfflineMode
- **GIVEN** Network requests in progress
- **WHEN** setOffline(true) called
- **THEN** Requests fail with network error, page shows offline

#### Scenario: RestoreOnlineConnectivity
- **GIVEN** Offline mode enabled
- **WHEN** setOffline(false) called
- **THEN** New requests succeed normally

### Requirement: Setrequestinterception

The system SHALL enables or disables request interception for a session, allowing modification of requests before they are sent.

#### Scenario: EnableInterception
- **GIVEN** Session exists
- **WHEN** setRequestInterception(true) called
- **THEN** Protocol request interception enabled for all requests

### Requirement: Setextrahttpheaders

The system SHALL adds custom http headers to all outgoing requests.

#### Scenario: AddCustomHeaders
- **GIVEN** Page making requests
- **WHEN** setExtraHTTPHeaders({ 'X-Custom': 'value' }) called
- **THEN** All subsequent requests include custom header

### Requirement: Clearcache

The system SHALL clears the browser's network cache for the current context.

#### Scenario: CacheInvalidation
- **GIVEN** Cached resources exist
- **WHEN** clearCache() called
- **THEN** Cache cleared, next requests fetch fresh resources

### Requirement: Processresponse

The system SHALL processes incoming http response data, updates request state with headers and body information.

#### Scenario: SuccessfulResponseProcessing
- **GIVEN** HTTP response received from server
- **WHEN** processResponse called with response data
- **THEN** Request object updated with status, headers, timing

### Requirement: Loadingfinished

The system SHALL handles completion of a network request, updates transfer sizes and timing metrics.

#### Scenario: RequestCompletionTracking
- **GIVEN** Request completed successfully
- **WHEN** loadingFinished event received
- **THEN** Transfer size recorded, request marked complete

### Requirement: Loadingfailed

The system SHALL handles failed network requests, records error information.

#### Scenario: NetworkErrorHandling
- **GIVEN** Request fails (DNS, connection refused, timeout)
- **WHEN** loadingFailed event received
- **THEN** Request marked failed with error code and message

### Requirement: Requestwillbesentextrainfo

The system SHALL processes additional request information before the request is sent.

#### Scenario: PreRequestIntercept
- **GIVEN** Request about to be sent with auth/cookies
- **WHEN** requestWillBeSentExtraInfo called
- **THEN** Cookies/auth headers attached to request context

### Requirement: Requestservedfromcache

The system SHALL marks a request as served from browser cache.

#### Scenario: CacheHitDetection
- **GIVEN** Request can be fulfilled from cache
- **WHEN** requestServedFromCache called
- **THEN** Request marked as servedFromCache=true

### Requirement: Responsereceivedextrainfo

The system SHALL processes additional response information including cookies and security details.

#### Scenario: ResponseExtraInfoProcessing
- **GIVEN** Response with Set-Cookie headers
- **WHEN** responseReceivedExtraInfo called
- **THEN** Cookies extracted and set on browser context

### Requirement: Continue

The system SHALL continues a intercepted request with optional overrides (method, url, headers, post data).

#### Scenario: ContinueWithModifiedHeaders
- **GIVEN** Request intercepted
- **WHEN** continue({ headers: { 'X-Modified': 'true' } }) called
- **THEN** Request sent with modified headers

#### Scenario: ContinueWithModifiedMethod
- **GIVEN** POST request intercepted
- **WHEN** continue({ method: 'GET' }) called
- **THEN** Request changed to GET method

### Requirement: Fulfill

The system SHALL fulfills an intercepted request with a custom response without hitting the network.

#### Scenario: MockApiResponse
- **GIVEN** API request intercepted
- **WHEN** fulfill({ status: 200, body: JSON.stringify(mockData) }) called
- **THEN** Request resolved with mock data, network not contacted

#### Scenario: ServeLocalFileAsResponse
- **GIVEN** Resource request intercepted
- **WHEN** fulfill({ path: '/local/file.html' }) called
- **THEN** File contents served as response body

### Requirement: Abort

The system SHALL aborts an intercepted request with a specific error code.

#### Scenario: BlockResourceLoading
- **GIVEN** Image request intercepted
- **WHEN** abort('failed') called
- **THEN** Request failed with network error

### Requirement: Onwebsocketcreated

The system SHALL handles the event when a new websocket connection is initiated.

#### Scenario: WebsocketConnectionStart
- **GIVEN** Page opens WebSocket connection
- **WHEN** WebSocket created event received
- **THEN** WebSocket tracking entry created

### Requirement: Onwebsocketrequest

The system SHALL handles websocket handshake request.

#### Scenario: WebsocketHandshake
- **GIVEN** WebSocket upgrade request
- **WHEN** WebSocket request event received
- **THEN** Handshake initiated

### Requirement: Onwebsocketresponse

The system SHALL handles websocket handshake response.

#### Scenario: WebsocketHandshakeResponse
- **GIVEN** Server sends 101 Switching Protocols
- **WHEN** WebSocket response event received
- **THEN** Connection upgraded to WebSocket

### Requirement: Onwebsocketframesent

The system SHALL tracks outgoing websocket data frames.

#### Scenario: OutgoingMessageTracking
- **GIVEN** Page sends WebSocket message
- **WHEN** Frame sent event received
- **THEN** Message logged in WebSocket event stream

### Requirement: Websocketframereceived

The system SHALL tracks incoming websocket data frames.

#### Scenario: IncomingMessageTracking
- **GIVEN** Server sends WebSocket message
- **WHEN** Frame received event received
- **THEN** Message logged in WebSocket event stream

### Requirement: Websocketclosed

The system SHALL handles websocket connection closure.

#### Scenario: ConnectionCloseHandling
- **GIVEN** WebSocket connection closed by either party
- **WHEN** WebSocket closed event received
- **THEN** Connection marked as closed, cleanup performed

### Requirement: Websocketerror

The system SHALL handles websocket protocol errors.

#### Scenario: WebsocketErrorHandling
- **GIVEN** WebSocket protocol error occurs
- **WHEN** WebSocket error event received
- **THEN** Error propagated to caller

### Requirement: Getresponsebody

The system SHALL retrieves the response body for a completed request.

#### Scenario: RetrieveJsonResponse
- **GIVEN** Request completed with JSON body
- **WHEN** getResponseBody called
- **THEN** Response body buffer returned

#### Scenario: RetrieveBinaryResponse
- **GIVEN** Request completed with binary body
- **WHEN** getResponseBody called
- **THEN** Binary buffer returned

### Requirement: Servedfromcache

The system SHALL determines if a request was served from browser cache.

#### Scenario: CheckCacheStatus
- **GIVEN** Request exists in tracking
- **WHEN** servedFromCache called with requestId
- **THEN** Returns true if served from cache

## Technical Notes

- **Implementation**: `ai_service/node_modules/playwright-core/types/protocol.d.ts`
- **Dependencies**: Chromium, Firefox, WebKit, Electron, Android, BidiChromium, BidiFirefox, AdbBackend, DebugController, Instrumentation, Playwright class, SdkObject (instrumentation), BrowserContext, Download, Artifact, Page, ClientCertificatesProxy, EventEmitter, RecorderService, BidiInterceptedRequest, BidiResponse, BidiSession, NetworkSession, FrameManager, ProtocolClient, NetworkManager
