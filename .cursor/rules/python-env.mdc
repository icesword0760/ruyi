---
description: 本项目 Python 环境规则：使用 conda omni 环境启动服务和安装依赖
alwaysApply: true
---

# Python 环境：conda omni

本项目的所有 Python 操作必须使用 conda `omni` 环境。

## Python 路径

```
/opt/anaconda3/envs/omni/bin/python
```

## 启动服务

```bash
# Flask 主服务
/opt/anaconda3/envs/omni/bin/python server.py

# OCR 服务
/opt/anaconda3/envs/omni/bin/python ai_service/ocr_service.py
```

## 安装依赖

```bash
# 安装包
/opt/anaconda3/envs/omni/bin/pip install <package>

# 批量安装
/opt/anaconda3/envs/omni/bin/pip install -r requirements.txt
```

## 禁止使用

- `python` / `python3`（可能指向 base 或系统环境）
- `pip install`（不带完整路径）
- `conda run -n base ...`
